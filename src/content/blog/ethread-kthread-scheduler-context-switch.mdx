---
title: "Threads, KTHREAD, and the Windows Scheduler"
description: "A plain-language, evidence-first guide to Threads, KTHREAD, and the Windows Scheduler."
locale: "en"
publishDate: 2026-07-28
updatedAt: 2026-07-28
draft: false
featured: false
tags:
  - "Windows Internals"
  - "processes execution"
  - "Research Notes"
author: "Kwpwn"
content_type: "concept"
status: "preliminary"
evidence_level: "unverified"
toc: true
wide: false
topic: "windows-internals"
track: "processes-execution"
seriesOrder: 3
topicPlacements:
  - topic: "windows-internals"
    track: "processes-execution"
    order: 3
difficulty: "Intermediate"
prerequisites:
  - "Basic Windows processes, threads, tokens, and handles"
  - "A disposable VM with matching public symbols"
learningObjectives:
  - "Build a clear mental model for Threads, KTHREAD, and the Windows Scheduler"
  - "Identify the state, decision boundary, required condition, and observable proof"
  - "Verify the model with a read-only probe and a meaningful negative control"
labEnvironment: "Use a disposable Windows debugging VM with matching Microsoft public symbols. Begin with user-mode inspection and documented APIs. Kernel debugger commands are read-only and should never be run against a production system. Record the exact OS build before interpreting undocumented structures."
---

> **Quick answer:** ETHREAD layers executive thread state around the scheduler-oriented KTHREAD. The conclusion becomes defensible only when the remaining boundary, constraint, and evidence stages are verified on the named build and identity.

## Scope and evidence contract

This lesson builds a mechanism-first model of Windows. Public contracts, observable implementation behavior, and version-specific internal fields are kept separate. Structures and debugger output are evidence for a particular build, not timeless ABI promises. Applied to **Threads, KTHREAD, and the Windows Scheduler**, this method begins with the concrete statement that ethread layers executive thread state around the scheduler-oriented kthread.

This document treats **Threads, KTHREAD, and the Windows Scheduler** as a mechanism to reconstruct, not a label to memorize. Its curriculum placement is windows-internals lesson 03 in processes-execution. That placement helps navigation; it does not imply that the behavior belongs to one security domain or that every environment exposes the same path.

The **Threads, KTHREAD, and the Windows Scheduler** track-specific focus is: Follow identity and lifetime separately: a process owns an address space and handle table, a thread is the schedulable execution context, and either object can outlive a user-visible identifier through retained references. The practical objective for this article is to explain who owns the state, which object carries it, which reference keeps it alive, and which transition a scheduler or executive component performs.

Before using any internal field, configuration value, event, or public write-up as evidence for **Threads, KTHREAD, and the Windows Scheduler**, record the target's Windows edition, version, build and revision, architecture, symbol status, relevant policy and mitigation state, execution identity, and collection time. The confidence vocabulary below prevents this specific lesson from hiding uncertainty behind polished prose:

| State | Meaning in this lesson |
| --- | --- |
| **CONFIRMED** | Current-target evidence proves the claim and the expected negative control changes the result. |
| **LIKELY** | Multiple sources support the claim, but one decisive low-risk observation remains unavailable. |
| **UNVERIFIED** | The statement is plausible but depends on public references, a decompiler view, a scanner label, another build, or an untested configuration. |
| **REJECTED** | Current evidence or a control contradicts the proposed mechanism. |

## Architecture at a glance

Use this flow as the shortest complete map:

> **owner or manager → object/state → transition → lifetime rule → version-matched observation**

Documented contracts, symbol-backed observations, and reverse-engineered implementation details have different stability. Keep those evidence classes separate. In **Threads, KTHREAD, and the Windows Scheduler**, the four stages resolve to owned state → manager or boundary → versioned constraint → observable evidence, and each stage must point to the mechanism statement and evidence in the table below.

| Stage | Mechanism on this topic | Strongest first evidence |
| --- | --- | --- |
| 1. **Owned state** | ETHREAD layers executive thread state around the scheduler-oriented KTHREAD. | Documented process and thread query APIs |
| 2. **Manager or boundary** | readiness, standby, running, waiting, transition, and termination states interact with priority, quantum, affinity, ideal processor, and heterogeneous scheduling policy. | Version-matched WinDbg process, thread, token, wait, and stack views |
| 3. **Versioned constraint** | a context switch saves architectural state but does not itself imply a security-context change. | Kernel process/thread/image ETW with loss counters retained |
| 4. **Observable evidence** | thread investigation correlates wait reason, wait blocks, locks, IRQL, APC state, stack, owning process, token impersonation, and build-specific fields. | Handle, reference, cancellation, and teardown observations |

For **Threads, KTHREAD, and the Windows Scheduler**, the table is a causal chain rather than a checklist of unrelated facts. Trace it backward from each observable evidence observation to the actor and owned state, then forward through the manager or boundary decision, transition, cleanup, and failure path.

## Mechanism deep dive

### 1. Owned state

**Mechanism claim.** ETHREAD layers executive thread state around the scheduler-oriented KTHREAD.

For **Threads, KTHREAD, and the Windows Scheduler**, this stage is where the write-up must become concrete. Name the executive manager, subsystem, process, driver, service, or runtime that owns the state; then separate that state from the handle or identifier used to find it. The relevant evidence source for this stage is **documented process and thread query apis**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this owned state stage.** For **Threads, KTHREAD, and the Windows Scheduler**, The displayed identifier, handle, field, or list entry refers to a different object or a stale/reused lifetime. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for owned state.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **Threads, KTHREAD, and the Windows Scheduler**? If not, this specific mechanism claim remains **UNVERIFIED**.

### 2. Manager or boundary

**Mechanism claim.** readiness, standby, running, waiting, transition, and termination states interact with priority, quantum, affinity, ideal processor, and heterogeneous scheduling policy.

For **Threads, KTHREAD, and the Windows Scheduler**, this stage is where the write-up must become concrete. Trace creation, lookup, access, mutation, queueing, completion, reference ownership, cancellation, and teardown across the actual subsystem boundary. The relevant evidence source for this stage is **version-matched windbg process, thread, token, wait, and stack views**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this manager or boundary stage.** For **Threads, KTHREAD, and the Windows Scheduler**, The presumed manager does not perform the transition, or another layer owns the decisive state and synchronization. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for manager or boundary.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **Threads, KTHREAD, and the Windows Scheduler**? If not, this specific mechanism claim remains **UNVERIFIED**.

### 3. Versioned constraint

**Mechanism claim.** a context switch saves architectural state but does not itself imply a security-context change.

For **Threads, KTHREAD, and the Windows Scheduler**, this stage is where the write-up must become concrete. Record build, architecture, symbols, policy, mitigation state, and every undocumented layout assumption before interpreting internal fields. The relevant evidence source for this stage is **kernel process/thread/image etw with loss counters retained**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this versioned constraint stage.** For **Threads, KTHREAD, and the Windows Scheduler**, The layout, policy, mitigation, architecture, or code path does not match the tested Windows build. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for versioned constraint.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **Threads, KTHREAD, and the Windows Scheduler**? If not, this specific mechanism claim remains **UNVERIFIED**.

### 4. Observable evidence

**Mechanism claim.** thread investigation correlates wait reason, wait blocks, locks, IRQL, APC state, stack, owning process, token impersonation, and build-specific fields.

For **Threads, KTHREAD, and the Windows Scheduler**, this stage is where the write-up must become concrete. Correlate a documented API or contract with debugger, trace, object, or memory evidence and retain enough provenance to reproduce the observation. The relevant evidence source for this stage is **handle, reference, cancellation, and teardown observations**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this observable evidence stage.** For **Threads, KTHREAD, and the Windows Scheduler**, A documented query, independent trace, or negative control contradicts the debugger-derived interpretation. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for observable evidence.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **Threads, KTHREAD, and the Windows Scheduler**? If not, this specific mechanism claim remains **UNVERIFIED**.

## Key terms in plain language

The terms below are intentionally scoped to **Threads, KTHREAD, and the Windows Scheduler**. They should not be used as synonyms: each names a different object, policy, boundary, or kind of evidence.

| Term | Precise working meaning |
| --- | --- |
| **Impersonation** | A thread temporarily evaluates security using a client token while the process primary token remains a separate security context. |
| **IRQL** | A per-processor interrupt-priority level that constrains scheduling, synchronization, interruptibility, and pageable-code access. |
| **APC** | A queued callback delivered in a particular thread context under specific kernel, special-kernel, or user-mode rules. |
| **Mechanism** | The concrete state, component, decision, and transition that produce an observable result. |
| **Trust boundary** | A point where data or control crosses into a component with different authority, ownership, or assumptions. |
| **Evidence** | A recorded observation with enough identity, time, build, provenance, and collection context to support or challenge a claim. |

When public documentation and debugger terminology for **Threads, KTHREAD, and the Windows Scheduler** differ, retain both names and state which source supplied each one. Any debugger field used in this lesson is evidence for the recorded build, not automatically a stable ABI or security contract.

## Version and environment scope

EPROCESS, ETHREAD, KTHREAD, worker-factory, scheduler, and mitigation fields are implementation details. Record the exact build, architecture, and symbol provenance before naming a field or offset. For **Threads, KTHREAD, and the Windows Scheduler**, the version-sensitive statement that must be rechecked first is: a context switch saves architectural state but does not itself imply a security-context change.

Record the following **Threads, KTHREAD, and the Windows Scheduler** dimensions before comparing two systems or importing an exploitability conclusion from another target:

| Dimension | Minimum record | Why it can change the answer |
| --- | --- | --- |
| Operating system | Edition, version, build, revision, update level | Code paths, defaults, layouts, services, telemetry, and mitigations change through servicing. |
| Architecture | x86, x64, ARM64, native or WoW64 view | Pointer width, calling convention, structure view, redirection, and available mitigations differ. |
| Identity | User and group SIDs, integrity, privileges, process token, thread token, session | The actor that can observe a condition may not be the actor that can reach or trigger it. |
| Policy | Effective local/domain/MDM/runtime source and enforcement mode | A visible registry value or UI setting may be overridden, merged, audited, or inactive. |
| Mitigations | VBS/HVCI, CFG/CET, DEP, ASLR, application control, PPL, sandbox policy as relevant | Exploitability and observability depend on actual opt-in, hardware, boot, and process state. |
| Symbols and tools | PDB identity, symbol path, debugger/parser/collector version | Offsets, names, schemas, and decoded fields are meaningful only with matching provenance. |
| Sensor health | Provider/session/channel, filters, loss, clock, retention, forwarding | Missing telemetry cannot support absence until a benign control proves the collection path. |
| Lifetime | Creation, references, asynchronous work, cancellation, teardown, cleanup | Stale handles, reused identifiers, queued callbacks, and delayed consumers can change object identity. |

Unknown values in a **Threads, KTHREAD, and the Windows Scheduler** decision record must remain explicitly **unknown**. Do not fill this article's gaps with defaults copied from documentation or another researcher's machine.

## Evidence matrix

For **Threads, KTHREAD, and the Windows Scheduler**, work from each mechanism claim to the least-invasive evidence source before choosing a tool:

| Claim stage | Preferred evidence | Observation that supports the claim | Observation that falsifies or narrows it |
| --- | --- | --- | --- |
| Owned state | Documented process and thread query APIs | ETHREAD layers executive thread state around the scheduler-oriented KTHREAD. | The displayed identifier, handle, field, or list entry refers to a different object or a stale/reused lifetime. |
| Manager or boundary | Version-matched WinDbg process, thread, token, wait, and stack views | readiness, standby, running, waiting, transition, and termination states interact with priority, quantum, affinity, ideal processor, and heterogeneous scheduling policy. | The presumed manager does not perform the transition, or another layer owns the decisive state and synchronization. |
| Versioned constraint | Kernel process/thread/image ETW with loss counters retained | a context switch saves architectural state but does not itself imply a security-context change. | The layout, policy, mitigation, architecture, or code path does not match the tested Windows build. |
| Observable evidence | Handle, reference, cancellation, and teardown observations | thread investigation correlates wait reason, wait blocks, locks, IRQL, APC state, stack, owning process, token impersonation, and build-specific fields. | A documented query, independent trace, or negative control contradicts the debugger-derived interpretation. |

For **Threads, KTHREAD, and the Windows Scheduler**, a screenshot of one promising field is weaker than a reproducible evidence bundle. Preserve raw commands, complete output, exit status, actor token, object identity, timestamps, build, symbol or decoder provenance, and both controls named above.

## Safe guided lab

Use a disposable Windows debugging VM with matching Microsoft public symbols. Begin with user-mode inspection and documented APIs. Kernel debugger commands are read-only and should never be run against a production system. Record the exact OS build before interpreting undocumented structures. The **Threads, KTHREAD, and the Windows Scheduler** exercise is limited to the read-only inspection probe and controls described below.

### 1. Establish the baseline

Run the **Threads, KTHREAD, and the Windows Scheduler** baseline from the identity and architecture named in the hypothesis. Use a fresh snapshot or purpose-built test object for this mechanism and save the output with an ISO-8601 timestamp.

```powershell
# Establish version and symbol context before interpreting fields
Get-ComputerInfo | Select-Object WindowsProductName, WindowsVersion, OsBuildNumber
Get-CimInstance Win32_OperatingSystem | Select-Object Version, BuildNumber
# In WinDbg: verify symbols, then use read-only inspection
.symfix
.reload /f
vertarget
```

Baseline acceptance criteria for **Threads, KTHREAD, and the Windows Scheduler**:

- the **Threads, KTHREAD, and the Windows Scheduler** execution identity and Windows build are visible in the saved record;
- the test object, file, process, endpoint, provider, or specimen belongs to the lab;
- relevant policy and mitigation values are captured before the observation;
- no production secret, unknown binary, persistence payload, or destructive trigger is introduced.

### 2. Inspect the topic-specific mechanism

The **Threads, KTHREAD, and the Windows Scheduler** probe below is read-only. Replace placeholders only with a process, file, object, trace, or VM that belongs to the authorized lab. If an elevated debugger is needed to observe this mechanism, record that elevation separately; it does not prove a standard user can cause the same transition.

```text
!thread <thread-address>
dt nt!_ETHREAD <thread-address>
!ready
!running -ti
```

Interpret the **Threads, KTHREAD, and the Windows Scheduler** output against owned state, manager or boundary, versioned constraint, observable evidence rather than a tool's severity label. If a field or command is unavailable on this target, record that as a version/tool result and do not import the missing value from another build.

### 3. Run a positive and negative control

For **Threads, KTHREAD, and the Windows Scheduler**, the positive control keeps every required precondition and predicts the observable evidence observation. The negative control changes exactly one relevant variable—actor identity, object ACL, policy, architecture/view, consumer state, trigger, network response, or sensor configuration—while the remaining lab state stays stable.

| Control | Change | Expected result |
| --- | --- | --- |
| Positive | Preserve all four mechanism stages | The predicted transition appears in the primary and at least one corroborating source. |
| Negative | Remove one required condition | The transition disappears or changes in the way predicted by the model. |
| Sensor health | Generate a benign event with a known result | The collector records and decodes it without unexplained loss. |
| Cleanup | Revert the test object and repeat inspection | Persistent state returns to baseline and no queued work remains. |

### 4. Write the decision record

```text
Title: Threads, KTHREAD, and the Windows Scheduler
Claim:
Target: edition | version | build.revision | architecture
Identity: process token | thread token | integrity | privileges | session
Starting state:
Decision boundary:
Required condition:
Observed transition:
Primary evidence:
Corroborating evidence:
Contradictory evidence:
Benign control:
Negative control:
Version and policy caveats:
Cleanup result:
Confidence: CONFIRMED | LIKELY | UNVERIFIED | REJECTED
Next experiment and falsification criterion:
```

The **Threads, KTHREAD, and the Windows Scheduler** lab is complete only when the controls explain its evidence. A dramatic tool label, isolated event, writable path, suspicious string, or debugger field cannot substitute for the four-stage mechanism.

## Worked case-study method

A debugger command displays an interesting field and the first interpretation treats it as a universal Windows rule. For **Threads, KTHREAD, and the Windows Scheduler**, treat that clue as a lead and review it through the same four stages:

1. **Owned state:** verify the current-target statement that ETHREAD layers executive thread state around the scheduler-oriented KTHREAD.
2. **Manager or boundary:** identify the component and decision described by this claim: readiness, standby, running, waiting, transition, and termination states interact with priority, quantum, affinity, ideal processor, and heterogeneous scheduling policy.
3. **Versioned constraint:** test the build, identity, policy, lifetime, and configuration constraint: a context switch saves architectural state but does not itself imply a security-context change.
4. **Observable evidence:** demand the joined observation described here: thread investigation correlates wait reason, wait blocks, locks, IRQL, APC state, stack, owning process, token impersonation, and build-specific fields.

For **Threads, KTHREAD, and the Windows Scheduler**, stop at the strongest supported conclusion. If owned state and manager or boundary are proven but versioned constraint is not, report a **dangerous condition** or **research lead**, not confirmed impact. If runtime evidence agrees but the sensor-health control fails, lower confidence. If the negative control does not change the result, revise this mechanism before collecting more screenshots.

A publication-quality **Threads, KTHREAD, and the Windows Scheduler** case study should include a target table, causal diagram, exact evidence excerpts, competing explanations, version scope, mitigation interaction, cleanup, limitations, and direct references. A second researcher must be able to reproduce this mechanism without relying on the original author's private intuition.

## Detection and hardening

- For **Threads, KTHREAD, and the Windows Scheduler**, use version-matched symbols and retain the exact build with every debugger observation.
- Correlate public API results, object state, and trace output instead of trusting one internal field.
- Track creation, references, asynchronous work, cancellation, and teardown as one lifecycle.
- Treat a missing field or command on another build as a version result, not permission to guess.

For the **Threads, KTHREAD, and the Windows Scheduler** detection model, correlate documented process and thread query apis, version-matched windbg process, thread, token, wait, and stack views, kernel process/thread/image etw with loss counters retained, handle, reference, cancellation, and teardown observations. Join its state change to consumer activity and resulting effect within a justified time window, then document required fields, clock source, exclusions, expected volume, sensor-health control, and what this analytic cannot prove.

Hardening **Threads, KTHREAD, and the Windows Scheduler** should break the earliest edge that is both reachable and operationally safe to change. After changing the relevant permission, policy, service exposure, execution control, authentication, egress, or telemetry path, repeat this lesson's controls. **Mitigated** means the predicted transition no longer occurs under the original preconditions—not merely that a setting changed.

## Failure modes and review checklist

Use this checklist when reviewing a draft of **Threads, KTHREAD, and the Windows Scheduler**:

- Does the opening of **Threads, KTHREAD, and the Windows Scheduler** state the exact target, actor, object or artifact, consumer, constraint, and result?
- Are documented facts, current-target observations, and implementation inferences labeled separately?
- Does every internal structure, event schema, default, or policy claim carry build and source provenance?
- Is a handle or identifier kept separate from the underlying object and its lifetime?
- Is writable or suspicious state kept separate from consumer reachability and actual execution?
- Are static capability, configured capability, and behavior observed in one execution reported at different confidence levels?
- Does a benign control prove the sensor, and does a negative control test causality?
- Are cancellation, teardown, cleanup, failure behavior, and asynchronous work included?
- Are mitigations recorded as effective target state rather than assumed from product version?
- Could another researcher falsify the conclusion using the retained evidence?

A **Threads, KTHREAD, and the Windows Scheduler** draft that cannot answer these questions is not finished, regardless of its word count or number of screenshots.

## Takeaways

- **Owned state:** ETHREAD layers executive thread state around the scheduler-oriented KTHREAD.
- **Manager or boundary:** readiness, standby, running, waiting, transition, and termination states interact with priority, quantum, affinity, ideal processor, and heterogeneous scheduling policy.
- **Versioned constraint:** a context switch saves architectural state but does not itself imply a security-context change.
- **Observable evidence:** thread investigation correlates wait reason, wait blocks, locks, IRQL, APC state, stack, owning process, token impersonation, and build-specific fields.

The durable **Threads, KTHREAD, and the Windows Scheduler** skill is not remembering one command. It is connecting each of this lesson's four mechanism statements to a target-specific observation, explaining what would disprove it, and repeating the test without silently changing the trust boundary.

## Check your understanding

<details>
  <summary>Can you explain Threads, KTHREAD, and the Windows Scheduler without naming a tool?</summary>
  A strong answer follows owner or manager → object/state → transition → lifetime rule → version-matched observation. It names the owner or actor, controlled state, deciding component, required condition, transition, lifetime, and evidence.
</details>

<details>
  <summary>Which fact is most likely to change across Windows versions?</summary>
  For **Threads, KTHREAD, and the Windows Scheduler**, start with this track warning: EPROCESS, ETHREAD, KTHREAD, worker-factory, scheduler, and mitigation fields are implementation details. Record the exact build, architecture, and symbol provenance before naming a field or offset. Then
  replace the assumption with a current-target observation tied to
  **ETHREAD layers executive thread state around the scheduler-oriented KTHREAD**.
</details>

<details>
  <summary>What result would make you reject the model?</summary>
  Reject or revise the **Threads, KTHREAD, and the Windows Scheduler** model when its predicted transition survives
  removal of a required condition, the stated actor cannot reach
  **readiness, standby, running, waiting, transition, and termination states interact with priority, quantum, affinity, ideal processor, and heterogeneous scheduling policy**, object identity or lifetime is wrong, or an
  independent healthy source contradicts the claimed state.
</details>

## Limitations and next research

This **Threads, KTHREAD, and the Windows Scheduler** document does not claim that every Windows edition, patch level, architecture, policy set, hardware configuration, or product integration behaves identically. Its read-only lab is not exploitability proof, attribution, or a production authorization decision. The next useful experiment is the smallest safe observation that can move one named owned state/manager or boundary/versioned constraint/observable evidence stage from **UNVERIFIED** to **LIKELY** or **CONFIRMED**, or reject it cleanly.

## References

### Source coverage and reliability

The **Threads, KTHREAD, and the Windows Scheduler** lesson consolidates the source material below. These pages and repositories provide context but cannot override current-target evidence. Any public structure layout, ReactOS/Wine behavior, debugger screenshot, blog-derived claim, or cross-build offset used here remains **UNVERIFIED** until checked against the tested binary, symbols, documented API, dump, or runtime trace.

| Source | Type | How it is used |
| --- | --- | --- |
| [Threads, KTHREAD, and the Windows Scheduler](https://yunolay.com/threads-kthread-and-the-windows-scheduler/) | Original lesson source | Establishes the source article's terminology and examples; re-check claims against primary documentation and the tested build. |
| [learn.microsoft.com/windows-hardware/drivers/](https://learn.microsoft.com/windows-hardware/drivers/) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [learn.microsoft.com/windows-hardware/drivers/debugger/](https://learn.microsoft.com/windows-hardware/drivers/debugger/) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [learn.microsoft.com/windows/win32/sysinfo/windows-system-information](https://learn.microsoft.com/windows/win32/sysinfo/windows-system-information) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [learn.microsoft.com/sysinternals/](https://learn.microsoft.com/sysinternals/) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [github.com/winsiderss/systeminformer/tree/master/phnt](https://github.com/winsiderss/systeminformer/tree/master/phnt) | Source repository | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [ntdoc.m417z.com/](https://ntdoc.m417z.com/) | Research reference | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [www.vergiliusproject.com/](https://www.vergiliusproject.com/) | Research reference | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [github.com/reactos/reactos](https://github.com/reactos/reactos) | Source repository | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |

Accessed: **2026-07-28**.
