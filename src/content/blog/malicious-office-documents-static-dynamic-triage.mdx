---
title: "Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction"
description: "A plain-language, evidence-first guide to Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction."
locale: "en"
publishDate: 2026-07-28
updatedAt: 2026-07-28
draft: false
featured: false
tags:
  - "Malware and C2 Analysis"
  - "static analysis"
  - "Research Notes"
author: "Kwpwn"
content_type: "concept"
status: "preliminary"
evidence_level: "unverified"
toc: true
wide: false
topic: "malware-c2"
track: "static-analysis"
seriesOrder: 4
topicPlacements:
  - topic: "malware-c2"
    track: "static-analysis"
    order: 4
difficulty: "Intermediate"
prerequisites:
  - "Basic Windows process, file, and network concepts"
  - "An approved isolated analysis environment"
learningObjectives:
  - "Build a clear mental model for Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction"
  - "Identify the state, decision boundary, required condition, and observable proof"
  - "Verify the model with a read-only probe and a meaningful negative control"
labEnvironment: "Use an approved isolated malware-analysis environment with snapshots, synthetic credentials, controlled networking, and non-production hardware. Prefer benign fixtures and inert byte sequences. Do not connect an unknown sample to the public Internet or implement a deployable payload, listener, persistence mechanism, or evasion chain."
---

> **Quick answer:** Modern Office files are ZIP-based Open XML packages while legacy compound files use OLE structured storage. The conclusion becomes defensible only when the remaining boundary, constraint, and evidence stages are verified on the named build and identity.

## Scope and evidence contract

This lesson separates observation from attribution. A sample or framework label is only an index. The useful model records state transitions across file structure, process execution, memory, persistence, network protocol, task semantics, and telemetry, while keeping every experiment inside a controlled analysis boundary. Applied to **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, this method begins with the concrete statement that modern office files are zip-based open xml packages while legacy compound files use ole structured storage.

This document treats **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** as a mechanism to reconstruct, not a label to memorize. Its curriculum placement is malware-c2 lesson 04 in static-analysis. That placement helps navigation; it does not imply that the behavior belongs to one security domain or that every environment exposes the same path.

The **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** track-specific focus is: Move from container and file structure to bounded capability claims. Validate offsets, sizes, encodings, imports, relocations, signatures, metadata, and parser assumptions before interpreting strings. The practical objective for this article is to explain what the artifact can support without claiming that a static capability executed during the observed incident.

Before using any internal field, configuration value, event, or public write-up as evidence for **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, record the target's Windows edition, version, build and revision, architecture, symbol status, relevant policy and mitigation state, execution identity, and collection time. The confidence vocabulary below prevents this specific lesson from hiding uncertainty behind polished prose:

| State | Meaning in this lesson |
| --- | --- |
| **CONFIRMED** | Current-target evidence proves the claim and the expected negative control changes the result. |
| **LIKELY** | Multiple sources support the claim, but one decisive low-risk observation remains unavailable. |
| **UNVERIFIED** | The statement is plausible but depends on public references, a decompiler view, a scanner label, another build, or an untested configuration. |
| **REJECTED** | Current evidence or a control contradicts the proposed mechanism. |

## Architecture at a glance

Use this flow as the shortest complete map:

> **artifact → parser or runtime → state transition → host/network evidence → bounded conclusion**

A family or framework label is an index, not an explanation. The useful result is the smallest behavior claim supported by healthy, time-aligned sensors. In **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, the four stages resolve to observable artifact → behavior boundary → execution constraint → correlated evidence, and each stage must point to the mechanism statement and evidence in the table below.

| Stage | Mechanism on this topic | Strongest first evidence |
| --- | --- | --- |
| 1. **Observable artifact** | Modern Office files are ZIP-based Open XML packages while legacy compound files use OLE structured storage. | Cryptographic hash, exact bytes, file type, container members, and provenance |
| 2. **Behavior boundary** | macros, relationships, embedded objects, templates, DDE-like links, and XLM sheets expose different execution and evidence paths. | Bounds-checked headers, sections, imports, exports, resources, and overlays |
| 3. **Execution constraint** | VBA stomping creates disagreement between source streams and compiled p-code and must be handled as an integrity signal. | Strings with encoding, location, references, and surrounding control flow |
| 4. **Correlated evidence** | analysis should start with structure and metadata, then use protected detonation only when static evidence cannot answer the question. | Compiler, library, packer, rule, and capability claims with hard negatives |

For **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, the table is a causal chain rather than a checklist of unrelated facts. Trace it backward from each correlated evidence observation to the actor and owned state, then forward through the behavior boundary decision, transition, cleanup, and failure path.

## Mechanism deep dive

### 1. Observable artifact

**Mechanism claim.** Modern Office files are ZIP-based Open XML packages while legacy compound files use OLE structured storage.

For **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, this stage is where the write-up must become concrete. Preserve the original artifact and describe the exact bytes, metadata, memory, request, or record that begins the hypothesis. The relevant evidence source for this stage is **cryptographic hash, exact bytes, file type, container members, and provenance**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this observable artifact stage.** For **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, The artifact is common compiler, library, administrative, or protocol material and does not carry the claimed behavior. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for observable artifact.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**? If not, this specific mechanism claim remains **UNVERIFIED**.

### 2. Behavior boundary

**Mechanism claim.** macros, relationships, embedded objects, templates, DDE-like links, and XLM sheets expose different execution and evidence paths.

For **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, this stage is where the write-up must become concrete. Identify the parser, loader, runtime, service, operating-system component, or protocol peer that turns the artifact into behavior. The relevant evidence source for this stage is **bounds-checked headers, sections, imports, exports, resources, and overlays**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this behavior boundary stage.** For **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, The expected parser, runtime, loader, service, or peer never consumes the artifact during the observed execution. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for behavior boundary.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**? If not, this specific mechanism claim remains **UNVERIFIED**.

### 3. Execution constraint

**Mechanism claim.** VBA stomping creates disagreement between source streams and compiled p-code and must be handled as an integrity signal.

For **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, this stage is where the write-up must become concrete. Record architecture, policy, privileges, configuration, timing, network responses, and anti-analysis conditions that select the observed path. The relevant evidence source for this stage is **strings with encoding, location, references, and surrounding control flow**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this execution constraint stage.** For **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, A required architecture, configuration, key, response, privilege, policy, or timing condition is absent. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for execution constraint.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**? If not, this specific mechanism claim remains **UNVERIFIED**.

### 4. Correlated evidence

**Mechanism claim.** analysis should start with structure and metadata, then use protected detonation only when static evidence cannot answer the question.

For **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, this stage is where the write-up must become concrete. Correlate independent host and network sources on one timeline and prove each sensor with a benign event before interpreting silence. The relevant evidence source for this stage is **compiler, library, packer, rule, and capability claims with hard negatives**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this correlated evidence stage.** For **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, Independent telemetry contradicts the transition, the sensor fails its health control, or the behavior cannot be repeated from a clean snapshot. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for correlated evidence.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**? If not, this specific mechanism claim remains **UNVERIFIED**.

## Key terms in plain language

The terms below are intentionally scoped to **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**. They should not be used as synonyms: each names a different object, policy, boundary, or kind of evidence.

| Term | Precise working meaning |
| --- | --- |
| **Mechanism** | The concrete state, component, decision, and transition that produce an observable result. |
| **Trust boundary** | A point where data or control crosses into a component with different authority, ownership, or assumptions. |
| **Evidence** | A recorded observation with enough identity, time, build, provenance, and collection context to support or challenge a claim. |
| **Negative control** | A repeat of the experiment with one required condition removed, used to test causality and alternate explanations. |

When public documentation and debugger terminology for **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** differ, retain both names and state which source supplied each one. Any debugger field used in this lesson is evidence for the recorded build, not automatically a stable ABI or security contract.

## Version and environment scope

Parser versions, format extensions, compiler output, signatures, YARA modules, packed variants, and document features change interpretation. Preserve tool versions and raw bytes. For **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, the version-sensitive statement that must be rechecked first is: VBA stomping creates disagreement between source streams and compiled p-code and must be handled as an integrity signal.

Record the following **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** dimensions before comparing two systems or importing an exploitability conclusion from another target:

| Dimension | Minimum record | Why it can change the answer |
| --- | --- | --- |
| Operating system | Edition, version, build, revision, update level | Code paths, defaults, layouts, services, telemetry, and mitigations change through servicing. |
| Architecture | x86, x64, ARM64, native or WoW64 view | Pointer width, calling convention, structure view, redirection, and available mitigations differ. |
| Identity | User and group SIDs, integrity, privileges, process token, thread token, session | The actor that can observe a condition may not be the actor that can reach or trigger it. |
| Policy | Effective local/domain/MDM/runtime source and enforcement mode | A visible registry value or UI setting may be overridden, merged, audited, or inactive. |
| Mitigations | VBS/HVCI, CFG/CET, DEP, ASLR, application control, PPL, sandbox policy as relevant | Exploitability and observability depend on actual opt-in, hardware, boot, and process state. |
| Symbols and tools | PDB identity, symbol path, debugger/parser/collector version | Offsets, names, schemas, and decoded fields are meaningful only with matching provenance. |
| Sensor health | Provider/session/channel, filters, loss, clock, retention, forwarding | Missing telemetry cannot support absence until a benign control proves the collection path. |
| Lifetime | Creation, references, asynchronous work, cancellation, teardown, cleanup | Stale handles, reused identifiers, queued callbacks, and delayed consumers can change object identity. |

Unknown values in a **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** decision record must remain explicitly **unknown**. Do not fill this article's gaps with defaults copied from documentation or another researcher's machine.

## Evidence matrix

For **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, work from each mechanism claim to the least-invasive evidence source before choosing a tool:

| Claim stage | Preferred evidence | Observation that supports the claim | Observation that falsifies or narrows it |
| --- | --- | --- | --- |
| Observable artifact | Cryptographic hash, exact bytes, file type, container members, and provenance | Modern Office files are ZIP-based Open XML packages while legacy compound files use OLE structured storage. | The artifact is common compiler, library, administrative, or protocol material and does not carry the claimed behavior. |
| Behavior boundary | Bounds-checked headers, sections, imports, exports, resources, and overlays | macros, relationships, embedded objects, templates, DDE-like links, and XLM sheets expose different execution and evidence paths. | The expected parser, runtime, loader, service, or peer never consumes the artifact during the observed execution. |
| Execution constraint | Strings with encoding, location, references, and surrounding control flow | VBA stomping creates disagreement between source streams and compiled p-code and must be handled as an integrity signal. | A required architecture, configuration, key, response, privilege, policy, or timing condition is absent. |
| Correlated evidence | Compiler, library, packer, rule, and capability claims with hard negatives | analysis should start with structure and metadata, then use protected detonation only when static evidence cannot answer the question. | Independent telemetry contradicts the transition, the sensor fails its health control, or the behavior cannot be repeated from a clean snapshot. |

For **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, a screenshot of one promising field is weaker than a reproducible evidence bundle. Preserve raw commands, complete output, exit status, actor token, object identity, timestamps, build, symbol or decoder provenance, and both controls named above.

## Safe guided lab

Use an approved isolated malware-analysis environment with snapshots, synthetic credentials, controlled networking, and non-production hardware. Prefer benign fixtures and inert byte sequences. Do not connect an unknown sample to the public Internet or implement a deployable payload, listener, persistence mechanism, or evasion chain. The **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** exercise is limited to the read-only inspection probe and controls described below.

### 1. Establish the baseline

Run the **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** baseline from the identity and architecture named in the hypothesis. Use a fresh snapshot or purpose-built test object for this mechanism and save the output with an ISO-8601 timestamp.

```powershell
# Static intake only; do not execute the specimen
$sample = ".\fixtures\owned-sample.bin"
Get-FileHash -Algorithm SHA256 $sample
Get-Item $sample | Select-Object Length, CreationTimeUtc, LastWriteTimeUtc
Get-AuthenticodeSignature $sample | Select-Object Status, StatusMessage
```

Baseline acceptance criteria for **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**:

- the **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** execution identity and Windows build are visible in the saved record;
- the test object, file, process, endpoint, provider, or specimen belongs to the lab;
- relevant policy and mitigation values are captured before the observation;
- no production secret, unknown binary, persistence payload, or destructive trigger is introduced.

### 2. Inspect the topic-specific mechanism

The **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** probe below is read-only. Replace placeholders only with a process, file, object, trace, or VM that belongs to the authorized lab. If an elevated debugger is needed to observe this mechanism, record that elevation separately; it does not prove a standard user can cause the same transition.

```text
Get-FileHash -Algorithm SHA256 .\fixture.docm
Expand-Archive .\fixture.docm .\expanded-doc -Force
Get-ChildItem .\expanded-doc -Recurse | Select-Object FullName, Length
```

Interpret the **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** output against observable artifact, behavior boundary, execution constraint, correlated evidence rather than a tool's severity label. If a field or command is unavailable on this target, record that as a version/tool result and do not import the missing value from another build.

### 3. Run a positive and negative control

For **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, the positive control keeps every required precondition and predicts the correlated evidence observation. The negative control changes exactly one relevant variable—actor identity, object ACL, policy, architecture/view, consumer state, trigger, network response, or sensor configuration—while the remaining lab state stays stable.

| Control | Change | Expected result |
| --- | --- | --- |
| Positive | Preserve all four mechanism stages | The predicted transition appears in the primary and at least one corroborating source. |
| Negative | Remove one required condition | The transition disappears or changes in the way predicted by the model. |
| Sensor health | Generate a benign event with a known result | The collector records and decodes it without unexplained loss. |
| Cleanup | Revert the test object and repeat inspection | Persistent state returns to baseline and no queued work remains. |

### 4. Write the decision record

```text
Title: Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction
Claim:
Target: edition | version | build.revision | architecture
Identity: process token | thread token | integrity | privileges | session
Starting state:
Decision boundary:
Required condition:
Observed transition:
Primary evidence:
Corroborating evidence:
Contradictory evidence:
Benign control:
Negative control:
Version and policy caveats:
Cleanup result:
Confidence: CONFIRMED | LIKELY | UNVERIFIED | REJECTED
Next experiment and falsification criterion:
```

The **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** lab is complete only when the controls explain its evidence. A dramatic tool label, isolated event, writable path, suspicious string, or debugger field cannot substitute for the four-stage mechanism.

## Worked case-study method

An analyst assigns a malware or C2 label from one string, process name, memory characteristic, or network indicator. For **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, treat that clue as a lead and review it through the same four stages:

1. **Observable artifact:** verify the current-target statement that Modern Office files are ZIP-based Open XML packages while legacy compound files use OLE structured storage.
2. **Behavior boundary:** identify the component and decision described by this claim: macros, relationships, embedded objects, templates, DDE-like links, and XLM sheets expose different execution and evidence paths.
3. **Execution constraint:** test the build, identity, policy, lifetime, and configuration constraint: VBA stomping creates disagreement between source streams and compiled p-code and must be handled as an integrity signal.
4. **Correlated evidence:** demand the joined observation described here: analysis should start with structure and metadata, then use protected detonation only when static evidence cannot answer the question.

For **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, stop at the strongest supported conclusion. If observable artifact and behavior boundary are proven but execution constraint is not, report a **dangerous condition** or **research lead**, not confirmed impact. If runtime evidence agrees but the sensor-health control fails, lower confidence. If the negative control does not change the result, revise this mechanism before collecting more screenshots.

A publication-quality **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** case study should include a target table, causal diagram, exact evidence excerpts, competing explanations, version scope, mitigation interaction, cleanup, limitations, and direct references. A second researcher must be able to reproduce this mechanism without relying on the original author's private intuition.

## Detection and hardening

- For **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, join file or memory provenance with the process and thread transition it caused.
- Correlate host behavior with DNS, flow, TLS, and application-protocol timing.
- Test every sensor with a benign control before interpreting silence.
- Harden execution, credential, egress, and logging boundaries independently.

For the **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** detection model, correlate cryptographic hash, exact bytes, file type, container members, and provenance, bounds-checked headers, sections, imports, exports, resources, and overlays, strings with encoding, location, references, and surrounding control flow, compiler, library, packer, rule, and capability claims with hard negatives. Join its state change to consumer activity and resulting effect within a justified time window, then document required fields, clock source, exclusions, expected volume, sensor-health control, and what this analytic cannot prove.

Hardening **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** should break the earliest edge that is both reachable and operationally safe to change. After changing the relevant permission, policy, service exposure, execution control, authentication, egress, or telemetry path, repeat this lesson's controls. **Mitigated** means the predicted transition no longer occurs under the original preconditions—not merely that a setting changed.

## Failure modes and review checklist

Use this checklist when reviewing a draft of **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**:

- Does the opening of **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** state the exact target, actor, object or artifact, consumer, constraint, and result?
- Are documented facts, current-target observations, and implementation inferences labeled separately?
- Does every internal structure, event schema, default, or policy claim carry build and source provenance?
- Is a handle or identifier kept separate from the underlying object and its lifetime?
- Is writable or suspicious state kept separate from consumer reachability and actual execution?
- Are static capability, configured capability, and behavior observed in one execution reported at different confidence levels?
- Does a benign control prove the sensor, and does a negative control test causality?
- Are cancellation, teardown, cleanup, failure behavior, and asynchronous work included?
- Are mitigations recorded as effective target state rather than assumed from product version?
- Could another researcher falsify the conclusion using the retained evidence?

A **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** draft that cannot answer these questions is not finished, regardless of its word count or number of screenshots.

## Takeaways

- **Observable artifact:** Modern Office files are ZIP-based Open XML packages while legacy compound files use OLE structured storage.
- **Behavior boundary:** macros, relationships, embedded objects, templates, DDE-like links, and XLM sheets expose different execution and evidence paths.
- **Execution constraint:** VBA stomping creates disagreement between source streams and compiled p-code and must be handled as an integrity signal.
- **Correlated evidence:** analysis should start with structure and metadata, then use protected detonation only when static evidence cannot answer the question.

The durable **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** skill is not remembering one command. It is connecting each of this lesson's four mechanism statements to a target-specific observation, explaining what would disprove it, and repeating the test without silently changing the trust boundary.

## Check your understanding

<details>
  <summary>Can you explain Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction without naming a tool?</summary>
  A strong answer follows artifact → parser or runtime → state transition → host/network evidence → bounded conclusion. It names the owner or actor, controlled state, deciding component, required condition, transition, lifetime, and evidence.
</details>

<details>
  <summary>Which fact is most likely to change across Windows versions?</summary>
  For **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction**, start with this track warning: Parser versions, format extensions, compiler output, signatures, YARA modules, packed variants, and document features change interpretation. Preserve tool versions and raw bytes. Then
  replace the assumption with a current-target observation tied to
  **Modern Office files are ZIP-based Open XML packages while legacy compound files use OLE structured storage**.
</details>

<details>
  <summary>What result would make you reject the model?</summary>
  Reject or revise the **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** model when its predicted transition survives
  removal of a required condition, the stated actor cannot reach
  **macros, relationships, embedded objects, templates, DDE-like links, and XLM sheets expose different execution and evidence paths**, object identity or lifetime is wrong, or an
  independent healthy source contradicts the claimed state.
</details>

## Limitations and next research

This **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** document does not claim that every Windows edition, patch level, architecture, policy set, hardware configuration, or product integration behaves identically. Its read-only lab is not exploitability proof, attribution, or a production authorization decision. The next useful experiment is the smallest safe observation that can move one named observable artifact/behavior boundary/execution constraint/correlated evidence stage from **UNVERIFIED** to **LIKELY** or **CONFIRMED**, or reject it cleanly.

## References

### Source coverage and reliability

The **Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction** lesson consolidates the source material below. These pages and repositories provide context but cannot override current-target evidence. Any public structure layout, ReactOS/Wine behavior, debugger screenshot, blog-derived claim, or cross-build offset used here remains **UNVERIFIED** until checked against the tested binary, symbols, documented API, dump, or runtime trace.

| Source | Type | How it is used |
| --- | --- | --- |
| [Dissecting Malicious Office Documents: VBA Macros, Stomping, and IOC Extraction](https://yunolay.com/malicious-office-documents/) | Original lesson source | Establishes the source article's terminology and examples; re-check claims against primary documentation and the tested build. |
| [learn.microsoft.com/windows/win32/debug/pe-format](https://learn.microsoft.com/windows/win32/debug/pe-format) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [learn.microsoft.com/sysinternals/downloads/sysmon](https://learn.microsoft.com/sysinternals/downloads/sysmon) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [learn.microsoft.com/defender-endpoint/](https://learn.microsoft.com/defender-endpoint/) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [attack.mitre.org/techniques/enterprise/](https://attack.mitre.org/techniques/enterprise/) | Knowledge base | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [yara.readthedocs.io/en/stable/](https://yara.readthedocs.io/en/stable/) | Research reference | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [github.com/mandiant/flare-vm](https://github.com/mandiant/flare-vm) | Source repository | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [github.com/REMnux](https://github.com/REMnux) | Source repository | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [github.com/mandiant/capa](https://github.com/mandiant/capa) | Source repository | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |

Accessed: **2026-07-28**.
