---
title: "Abusing BITS Jobs for Download and Persistence"
description: "A plain-language, evidence-first guide to Abusing BITS Jobs for Download and Persistence."
locale: "en"
publishDate: 2026-07-28
updatedAt: 2026-07-28
draft: false
featured: false
tags:
  - "Malware and C2 Analysis"
  - "execution persistence"
  - "Research Notes"
author: "Kwpwn"
content_type: "concept"
status: "preliminary"
evidence_level: "unverified"
toc: true
wide: false
topic: "malware-c2"
track: "execution-persistence"
seriesOrder: 12
topicPlacements:
  - topic: "malware-c2"
    track: "execution-persistence"
    order: 12
difficulty: "Advanced"
prerequisites:
  - "Basic Windows process, file, and network concepts"
  - "An approved isolated analysis environment"
learningObjectives:
  - "Build a clear mental model for Abusing BITS Jobs for Download and Persistence"
  - "Identify the state, decision boundary, required condition, and observable proof"
  - "Verify the model with a read-only probe and a meaningful negative control"
labEnvironment: "Use an approved isolated malware-analysis environment with snapshots, synthetic credentials, controlled networking, and non-production hardware. Prefer benign fixtures and inert byte sequences. Do not connect an unknown sample to the public Internet or implement a deployable payload, listener, persistence mechanism, or evasion chain."
---

> **Quick answer:** BITS persists transfer jobs through a service-managed queue and can continue work across user sessions and network changes. The conclusion becomes defensible only when the remaining boundary, constraint, and evidence stages are verified on the named build and identity.

## Scope and evidence contract

This lesson separates observation from attribution. A sample or framework label is only an index. The useful model records state transitions across file structure, process execution, memory, persistence, network protocol, task semantics, and telemetry, while keeping every experiment inside a controlled analysis boundary. Applied to **Abusing BITS Jobs for Download and Persistence**, this method begins with the concrete statement that bits persists transfer jobs through a service-managed queue and can continue work across user sessions and network changes.

This document treats **Abusing BITS Jobs for Download and Persistence** as a mechanism to reconstruct, not a label to memorize. Its curriculum placement is malware-c2 lesson 12 in execution-persistence. That placement helps navigation; it does not imply that the behavior belongs to one security domain or that every environment exposes the same path.

The **Abusing BITS Jobs for Download and Persistence** track-specific focus is: Separate initial execution, privilege context, persistence storage, trigger, consumer, asynchronous service state, transfer job, and later recurrence. The practical objective for this article is to prove recurrence under the intended identity and lifecycle event instead of labeling a stored artifact as established persistence.

Before using any internal field, configuration value, event, or public write-up as evidence for **Abusing BITS Jobs for Download and Persistence**, record the target's Windows edition, version, build and revision, architecture, symbol status, relevant policy and mitigation state, execution identity, and collection time. The confidence vocabulary below prevents this specific lesson from hiding uncertainty behind polished prose:

| State | Meaning in this lesson |
| --- | --- |
| **CONFIRMED** | Current-target evidence proves the claim and the expected negative control changes the result. |
| **LIKELY** | Multiple sources support the claim, but one decisive low-risk observation remains unavailable. |
| **UNVERIFIED** | The statement is plausible but depends on public references, a decompiler view, a scanner label, another build, or an untested configuration. |
| **REJECTED** | Current evidence or a control contradicts the proposed mechanism. |

## Architecture at a glance

Use this flow as the shortest complete map:

> **artifact → parser or runtime → state transition → host/network evidence → bounded conclusion**

A family or framework label is an index, not an explanation. The useful result is the smallest behavior claim supported by healthy, time-aligned sensors. In **Abusing BITS Jobs for Download and Persistence**, the four stages resolve to observable artifact → behavior boundary → execution constraint → correlated evidence, and each stage must point to the mechanism statement and evidence in the table below.

| Stage | Mechanism on this topic | Strongest first evidence |
| --- | --- | --- |
| 1. **Observable artifact** | BITS persists transfer jobs through a service-managed queue and can continue work across user sessions and network changes. | Writer and stored object with before/after state |
| 2. **Behavior boundary** | a job has owner, type, state, files, notification behavior, retry timing, and credentials or proxy context. | Task, service, BITS, WMI, registry, startup, or Linux consumer configuration |
| 3. **Execution constraint** | asynchronous execution makes process-only narratives incomplete. | Trigger time, process ancestry, token, network activity, and resulting artifacts |
| 4. **Correlated evidence** | defenders correlate BITS client operational events, job enumeration, qmgr storage artifacts, destination paths, notification commands, network destinations, and the creating principal. | Reboot/logon/session controls plus cleanup and recurrence verification |

For **Abusing BITS Jobs for Download and Persistence**, the table is a causal chain rather than a checklist of unrelated facts. Trace it backward from each correlated evidence observation to the actor and owned state, then forward through the behavior boundary decision, transition, cleanup, and failure path.

## Mechanism deep dive

### 1. Observable artifact

**Mechanism claim.** BITS persists transfer jobs through a service-managed queue and can continue work across user sessions and network changes.

For **Abusing BITS Jobs for Download and Persistence**, this stage is where the write-up must become concrete. Preserve the original artifact and describe the exact bytes, metadata, memory, request, or record that begins the hypothesis. The relevant evidence source for this stage is **writer and stored object with before/after state**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this observable artifact stage.** For **Abusing BITS Jobs for Download and Persistence**, The artifact is common compiler, library, administrative, or protocol material and does not carry the claimed behavior. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for observable artifact.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **Abusing BITS Jobs for Download and Persistence**? If not, this specific mechanism claim remains **UNVERIFIED**.

### 2. Behavior boundary

**Mechanism claim.** a job has owner, type, state, files, notification behavior, retry timing, and credentials or proxy context.

For **Abusing BITS Jobs for Download and Persistence**, this stage is where the write-up must become concrete. Identify the parser, loader, runtime, service, operating-system component, or protocol peer that turns the artifact into behavior. The relevant evidence source for this stage is **task, service, bits, wmi, registry, startup, or linux consumer configuration**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this behavior boundary stage.** For **Abusing BITS Jobs for Download and Persistence**, The expected parser, runtime, loader, service, or peer never consumes the artifact during the observed execution. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for behavior boundary.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **Abusing BITS Jobs for Download and Persistence**? If not, this specific mechanism claim remains **UNVERIFIED**.

### 3. Execution constraint

**Mechanism claim.** asynchronous execution makes process-only narratives incomplete.

For **Abusing BITS Jobs for Download and Persistence**, this stage is where the write-up must become concrete. Record architecture, policy, privileges, configuration, timing, network responses, and anti-analysis conditions that select the observed path. The relevant evidence source for this stage is **trigger time, process ancestry, token, network activity, and resulting artifacts**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this execution constraint stage.** For **Abusing BITS Jobs for Download and Persistence**, A required architecture, configuration, key, response, privilege, policy, or timing condition is absent. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for execution constraint.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **Abusing BITS Jobs for Download and Persistence**? If not, this specific mechanism claim remains **UNVERIFIED**.

### 4. Correlated evidence

**Mechanism claim.** defenders correlate BITS client operational events, job enumeration, qmgr storage artifacts, destination paths, notification commands, network destinations, and the creating principal.

For **Abusing BITS Jobs for Download and Persistence**, this stage is where the write-up must become concrete. Correlate independent host and network sources on one timeline and prove each sensor with a benign event before interpreting silence. The relevant evidence source for this stage is **reboot/logon/session controls plus cleanup and recurrence verification**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this correlated evidence stage.** For **Abusing BITS Jobs for Download and Persistence**, Independent telemetry contradicts the transition, the sensor fails its health control, or the behavior cannot be repeated from a clean snapshot. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for correlated evidence.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **Abusing BITS Jobs for Download and Persistence**? If not, this specific mechanism claim remains **UNVERIFIED**.

## Key terms in plain language

The terms below are intentionally scoped to **Abusing BITS Jobs for Download and Persistence**. They should not be used as synonyms: each names a different object, policy, boundary, or kind of evidence.

| Term | Precise working meaning |
| --- | --- |
| **Mechanism** | The concrete state, component, decision, and transition that produce an observable result. |
| **Trust boundary** | A point where data or control crosses into a component with different authority, ownership, or assumptions. |
| **Evidence** | A recorded observation with enough identity, time, build, provenance, and collection context to support or challenge a claim. |
| **Negative control** | A repeat of the experiment with one required condition removed, used to test causality and alternate explanations. |

When public documentation and debugger terminology for **Abusing BITS Jobs for Download and Persistence** differ, retain both names and state which source supplied each one. Any debugger field used in this lesson is evidence for the recorded build, not automatically a stable ABI or security contract.

## Version and environment scope

BITS queue format, WMI repository behavior, task/service policy, WSL architecture, systemd precedence, application control, and telemetry differ across environments. For **Abusing BITS Jobs for Download and Persistence**, the version-sensitive statement that must be rechecked first is: asynchronous execution makes process-only narratives incomplete.

Record the following **Abusing BITS Jobs for Download and Persistence** dimensions before comparing two systems or importing an exploitability conclusion from another target:

| Dimension | Minimum record | Why it can change the answer |
| --- | --- | --- |
| Operating system | Edition, version, build, revision, update level | Code paths, defaults, layouts, services, telemetry, and mitigations change through servicing. |
| Architecture | x86, x64, ARM64, native or WoW64 view | Pointer width, calling convention, structure view, redirection, and available mitigations differ. |
| Identity | User and group SIDs, integrity, privileges, process token, thread token, session | The actor that can observe a condition may not be the actor that can reach or trigger it. |
| Policy | Effective local/domain/MDM/runtime source and enforcement mode | A visible registry value or UI setting may be overridden, merged, audited, or inactive. |
| Mitigations | VBS/HVCI, CFG/CET, DEP, ASLR, application control, PPL, sandbox policy as relevant | Exploitability and observability depend on actual opt-in, hardware, boot, and process state. |
| Symbols and tools | PDB identity, symbol path, debugger/parser/collector version | Offsets, names, schemas, and decoded fields are meaningful only with matching provenance. |
| Sensor health | Provider/session/channel, filters, loss, clock, retention, forwarding | Missing telemetry cannot support absence until a benign control proves the collection path. |
| Lifetime | Creation, references, asynchronous work, cancellation, teardown, cleanup | Stale handles, reused identifiers, queued callbacks, and delayed consumers can change object identity. |

Unknown values in a **Abusing BITS Jobs for Download and Persistence** decision record must remain explicitly **unknown**. Do not fill this article's gaps with defaults copied from documentation or another researcher's machine.

## Evidence matrix

For **Abusing BITS Jobs for Download and Persistence**, work from each mechanism claim to the least-invasive evidence source before choosing a tool:

| Claim stage | Preferred evidence | Observation that supports the claim | Observation that falsifies or narrows it |
| --- | --- | --- | --- |
| Observable artifact | Writer and stored object with before/after state | BITS persists transfer jobs through a service-managed queue and can continue work across user sessions and network changes. | The artifact is common compiler, library, administrative, or protocol material and does not carry the claimed behavior. |
| Behavior boundary | Task, service, BITS, WMI, registry, startup, or Linux consumer configuration | a job has owner, type, state, files, notification behavior, retry timing, and credentials or proxy context. | The expected parser, runtime, loader, service, or peer never consumes the artifact during the observed execution. |
| Execution constraint | Trigger time, process ancestry, token, network activity, and resulting artifacts | asynchronous execution makes process-only narratives incomplete. | A required architecture, configuration, key, response, privilege, policy, or timing condition is absent. |
| Correlated evidence | Reboot/logon/session controls plus cleanup and recurrence verification | defenders correlate BITS client operational events, job enumeration, qmgr storage artifacts, destination paths, notification commands, network destinations, and the creating principal. | Independent telemetry contradicts the transition, the sensor fails its health control, or the behavior cannot be repeated from a clean snapshot. |

For **Abusing BITS Jobs for Download and Persistence**, a screenshot of one promising field is weaker than a reproducible evidence bundle. Preserve raw commands, complete output, exit status, actor token, object identity, timestamps, build, symbol or decoder provenance, and both controls named above.

## Safe guided lab

Use an approved isolated malware-analysis environment with snapshots, synthetic credentials, controlled networking, and non-production hardware. Prefer benign fixtures and inert byte sequences. Do not connect an unknown sample to the public Internet or implement a deployable payload, listener, persistence mechanism, or evasion chain. The **Abusing BITS Jobs for Download and Persistence** exercise is limited to the read-only inspection probe and controls described below.

### 1. Establish the baseline

Run the **Abusing BITS Jobs for Download and Persistence** baseline from the identity and architecture named in the hypothesis. Use a fresh snapshot or purpose-built test object for this mechanism and save the output with an ISO-8601 timestamp.

```powershell
# Static intake only; do not execute the specimen
$sample = ".\fixtures\owned-sample.bin"
Get-FileHash -Algorithm SHA256 $sample
Get-Item $sample | Select-Object Length, CreationTimeUtc, LastWriteTimeUtc
Get-AuthenticodeSignature $sample | Select-Object Status, StatusMessage
```

Baseline acceptance criteria for **Abusing BITS Jobs for Download and Persistence**:

- the **Abusing BITS Jobs for Download and Persistence** execution identity and Windows build are visible in the saved record;
- the test object, file, process, endpoint, provider, or specimen belongs to the lab;
- relevant policy and mitigation values are captured before the observation;
- no production secret, unknown binary, persistence payload, or destructive trigger is introduced.

### 2. Inspect the topic-specific mechanism

The **Abusing BITS Jobs for Download and Persistence** probe below is read-only. Replace placeholders only with a process, file, object, trace, or VM that belongs to the authorized lab. If an elevated debugger is needed to observe this mechanism, record that elevation separately; it does not prove a standard user can cause the same transition.

```text
Get-BitsTransfer -AllUsers -ErrorAction SilentlyContinue |
  Select-Object DisplayName, OwnerAccount, JobState, TransferType
Get-WinEvent -LogName Microsoft-Windows-Bits-Client/Operational -MaxEvents 30
```

Interpret the **Abusing BITS Jobs for Download and Persistence** output against observable artifact, behavior boundary, execution constraint, correlated evidence rather than a tool's severity label. If a field or command is unavailable on this target, record that as a version/tool result and do not import the missing value from another build.

### 3. Run a positive and negative control

For **Abusing BITS Jobs for Download and Persistence**, the positive control keeps every required precondition and predicts the correlated evidence observation. The negative control changes exactly one relevant variable—actor identity, object ACL, policy, architecture/view, consumer state, trigger, network response, or sensor configuration—while the remaining lab state stays stable.

| Control | Change | Expected result |
| --- | --- | --- |
| Positive | Preserve all four mechanism stages | The predicted transition appears in the primary and at least one corroborating source. |
| Negative | Remove one required condition | The transition disappears or changes in the way predicted by the model. |
| Sensor health | Generate a benign event with a known result | The collector records and decodes it without unexplained loss. |
| Cleanup | Revert the test object and repeat inspection | Persistent state returns to baseline and no queued work remains. |

### 4. Write the decision record

```text
Title: Abusing BITS Jobs for Download and Persistence
Claim:
Target: edition | version | build.revision | architecture
Identity: process token | thread token | integrity | privileges | session
Starting state:
Decision boundary:
Required condition:
Observed transition:
Primary evidence:
Corroborating evidence:
Contradictory evidence:
Benign control:
Negative control:
Version and policy caveats:
Cleanup result:
Confidence: CONFIRMED | LIKELY | UNVERIFIED | REJECTED
Next experiment and falsification criterion:
```

The **Abusing BITS Jobs for Download and Persistence** lab is complete only when the controls explain its evidence. A dramatic tool label, isolated event, writable path, suspicious string, or debugger field cannot substitute for the four-stage mechanism.

## Worked case-study method

An analyst assigns a malware or C2 label from one string, process name, memory characteristic, or network indicator. For **Abusing BITS Jobs for Download and Persistence**, treat that clue as a lead and review it through the same four stages:

1. **Observable artifact:** verify the current-target statement that BITS persists transfer jobs through a service-managed queue and can continue work across user sessions and network changes.
2. **Behavior boundary:** identify the component and decision described by this claim: a job has owner, type, state, files, notification behavior, retry timing, and credentials or proxy context.
3. **Execution constraint:** test the build, identity, policy, lifetime, and configuration constraint: asynchronous execution makes process-only narratives incomplete.
4. **Correlated evidence:** demand the joined observation described here: defenders correlate BITS client operational events, job enumeration, qmgr storage artifacts, destination paths, notification commands, network destinations, and the creating principal.

For **Abusing BITS Jobs for Download and Persistence**, stop at the strongest supported conclusion. If observable artifact and behavior boundary are proven but execution constraint is not, report a **dangerous condition** or **research lead**, not confirmed impact. If runtime evidence agrees but the sensor-health control fails, lower confidence. If the negative control does not change the result, revise this mechanism before collecting more screenshots.

A publication-quality **Abusing BITS Jobs for Download and Persistence** case study should include a target table, causal diagram, exact evidence excerpts, competing explanations, version scope, mitigation interaction, cleanup, limitations, and direct references. A second researcher must be able to reproduce this mechanism without relying on the original author's private intuition.

## Detection and hardening

- For **Abusing BITS Jobs for Download and Persistence**, join file or memory provenance with the process and thread transition it caused.
- Correlate host behavior with DNS, flow, TLS, and application-protocol timing.
- Test every sensor with a benign control before interpreting silence.
- Harden execution, credential, egress, and logging boundaries independently.

For the **Abusing BITS Jobs for Download and Persistence** detection model, correlate writer and stored object with before/after state, task, service, bits, wmi, registry, startup, or linux consumer configuration, trigger time, process ancestry, token, network activity, and resulting artifacts, reboot/logon/session controls plus cleanup and recurrence verification. Join its state change to consumer activity and resulting effect within a justified time window, then document required fields, clock source, exclusions, expected volume, sensor-health control, and what this analytic cannot prove.

Hardening **Abusing BITS Jobs for Download and Persistence** should break the earliest edge that is both reachable and operationally safe to change. After changing the relevant permission, policy, service exposure, execution control, authentication, egress, or telemetry path, repeat this lesson's controls. **Mitigated** means the predicted transition no longer occurs under the original preconditions—not merely that a setting changed.

## Failure modes and review checklist

Use this checklist when reviewing a draft of **Abusing BITS Jobs for Download and Persistence**:

- Does the opening of **Abusing BITS Jobs for Download and Persistence** state the exact target, actor, object or artifact, consumer, constraint, and result?
- Are documented facts, current-target observations, and implementation inferences labeled separately?
- Does every internal structure, event schema, default, or policy claim carry build and source provenance?
- Is a handle or identifier kept separate from the underlying object and its lifetime?
- Is writable or suspicious state kept separate from consumer reachability and actual execution?
- Are static capability, configured capability, and behavior observed in one execution reported at different confidence levels?
- Does a benign control prove the sensor, and does a negative control test causality?
- Are cancellation, teardown, cleanup, failure behavior, and asynchronous work included?
- Are mitigations recorded as effective target state rather than assumed from product version?
- Could another researcher falsify the conclusion using the retained evidence?

A **Abusing BITS Jobs for Download and Persistence** draft that cannot answer these questions is not finished, regardless of its word count or number of screenshots.

## Takeaways

- **Observable artifact:** BITS persists transfer jobs through a service-managed queue and can continue work across user sessions and network changes.
- **Behavior boundary:** a job has owner, type, state, files, notification behavior, retry timing, and credentials or proxy context.
- **Execution constraint:** asynchronous execution makes process-only narratives incomplete.
- **Correlated evidence:** defenders correlate BITS client operational events, job enumeration, qmgr storage artifacts, destination paths, notification commands, network destinations, and the creating principal.

The durable **Abusing BITS Jobs for Download and Persistence** skill is not remembering one command. It is connecting each of this lesson's four mechanism statements to a target-specific observation, explaining what would disprove it, and repeating the test without silently changing the trust boundary.

## Check your understanding

<details>
  <summary>Can you explain Abusing BITS Jobs for Download and Persistence without naming a tool?</summary>
  A strong answer follows artifact → parser or runtime → state transition → host/network evidence → bounded conclusion. It names the owner or actor, controlled state, deciding component, required condition, transition, lifetime, and evidence.
</details>

<details>
  <summary>Which fact is most likely to change across Windows versions?</summary>
  For **Abusing BITS Jobs for Download and Persistence**, start with this track warning: BITS queue format, WMI repository behavior, task/service policy, WSL architecture, systemd precedence, application control, and telemetry differ across environments. Then
  replace the assumption with a current-target observation tied to
  **BITS persists transfer jobs through a service-managed queue and can continue work across user sessions and network changes**.
</details>

<details>
  <summary>What result would make you reject the model?</summary>
  Reject or revise the **Abusing BITS Jobs for Download and Persistence** model when its predicted transition survives
  removal of a required condition, the stated actor cannot reach
  **a job has owner, type, state, files, notification behavior, retry timing, and credentials or proxy context**, object identity or lifetime is wrong, or an
  independent healthy source contradicts the claimed state.
</details>

## Limitations and next research

This **Abusing BITS Jobs for Download and Persistence** document does not claim that every Windows edition, patch level, architecture, policy set, hardware configuration, or product integration behaves identically. Its read-only lab is not exploitability proof, attribution, or a production authorization decision. The next useful experiment is the smallest safe observation that can move one named observable artifact/behavior boundary/execution constraint/correlated evidence stage from **UNVERIFIED** to **LIKELY** or **CONFIRMED**, or reject it cleanly.

## References

### Source coverage and reliability

The **Abusing BITS Jobs for Download and Persistence** lesson consolidates the source material below. These pages and repositories provide context but cannot override current-target evidence. Any public structure layout, ReactOS/Wine behavior, debugger screenshot, blog-derived claim, or cross-build offset used here remains **UNVERIFIED** until checked against the tested binary, symbols, documented API, dump, or runtime trace.

| Source | Type | How it is used |
| --- | --- | --- |
| [Abusing BITS Jobs for Download and Persistence](https://yunolay.com/abusing-bits-jobs-for-download-and-persistence/) | Original lesson source | Establishes the source article's terminology and examples; re-check claims against primary documentation and the tested build. |
| [learn.microsoft.com/windows/win32/debug/pe-format](https://learn.microsoft.com/windows/win32/debug/pe-format) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [learn.microsoft.com/sysinternals/downloads/sysmon](https://learn.microsoft.com/sysinternals/downloads/sysmon) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [learn.microsoft.com/defender-endpoint/](https://learn.microsoft.com/defender-endpoint/) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [attack.mitre.org/techniques/enterprise/](https://attack.mitre.org/techniques/enterprise/) | Knowledge base | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [yara.readthedocs.io/en/stable/](https://yara.readthedocs.io/en/stable/) | Research reference | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [github.com/mandiant/flare-vm](https://github.com/mandiant/flare-vm) | Source repository | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [github.com/REMnux](https://github.com/REMnux) | Source repository | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [github.com/mandiant/capa](https://github.com/mandiant/capa) | Source repository | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |

Accessed: **2026-07-28**.
