---
title: "Windows Subsystem for Linux (WSL) as an Attack Surface"
description: "A plain-language, evidence-first guide to Windows Subsystem for Linux (WSL) as an Attack Surface."
locale: "en"
publishDate: 2026-07-28
updatedAt: 2026-07-28
draft: false
featured: false
tags:
  - "Windows Privilege Escalation"
  - "lateral boundaries"
  - "Research Notes"
author: "Kwpwn"
content_type: "concept"
status: "preliminary"
evidence_level: "unverified"
toc: true
wide: false
topic: "windows-privesc"
track: "lateral-boundaries"
seriesOrder: 27
topicPlacements:
  - topic: "windows-privesc"
    track: "lateral-boundaries"
    order: 27
difficulty: "Intermediate"
prerequisites:
  - "Basic Windows access-control and process concepts"
  - "A disposable VM with a standard-user test account"
learningObjectives:
  - "Build a clear mental model for Windows Subsystem for Linux (WSL) as an Attack Surface"
  - "Identify the state, decision boundary, required condition, and observable proof"
  - "Verify the model with a read-only probe and a meaningful negative control"
labEnvironment: "Use a disposable Windows 11 virtual machine with a standard-user test account and a separate administrator account. Inspect only resources you create or systems you are explicitly authorized to assess. The exercises collect configuration and telemetry; they do not deploy an escalation payload."
---

> **Quick answer:** WSL 1 and WSL 2 have different architecture, kernel, filesystem, and network boundaries. The conclusion becomes defensible only when the remaining boundary, constraint, and evidence stages are verified on the named build and identity.

## Scope and evidence contract

This lesson treats local elevation as a verifiable trust-boundary chain. It does not begin with a payload. It begins with the caller's real token, the object the caller can influence, the privileged component that consumes that object, and the operation that would produce a stronger security context. Applied to **Windows Subsystem for Linux (WSL) as an Attack Surface**, this method begins with the concrete statement that wsl 1 and wsl 2 have different architecture, kernel, filesystem, and network boundaries.

This document treats **Windows Subsystem for Linux (WSL) as an Attack Surface** as a mechanism to reconstruct, not a label to memorize. Its curriculum placement is windows-privesc lesson 27 in lateral-boundaries. That placement helps navigation; it does not imply that the behavior belongs to one security domain or that every environment exposes the same path.

The **Windows Subsystem for Linux (WSL) as an Attack Surface** track-specific focus is: Separate identity coercion, transport authentication, relay compatibility, target authorization, remote execution or management semantics, and durable artifacts. The practical objective for this article is to stop at the strongest proven remote effect and do not equate a coerced authentication, successful relay, and code execution.

Before using any internal field, configuration value, event, or public write-up as evidence for **Windows Subsystem for Linux (WSL) as an Attack Surface**, record the target's Windows edition, version, build and revision, architecture, symbol status, relevant policy and mitigation state, execution identity, and collection time. The confidence vocabulary below prevents this specific lesson from hiding uncertainty behind polished prose:

| State | Meaning in this lesson |
| --- | --- |
| **CONFIRMED** | Current-target evidence proves the claim and the expected negative control changes the result. |
| **LIKELY** | Multiple sources support the claim, but one decisive low-risk observation remains unavailable. |
| **UNVERIFIED** | The statement is plausible but depends on public references, a decompiler view, a scanner label, another build, or an untested configuration. |
| **REJECTED** | Current evidence or a control contradicts the proposed mechanism. |

## Architecture at a glance

Use this flow as the shortest complete map:

> **lower-privileged actor → controlled input → privileged consumer → trigger → measurable effect**

A writable object is only the first edge. The chain is incomplete until the privileged consumer, trigger, resulting context, and negative control are all known. In **Windows Subsystem for Linux (WSL) as an Attack Surface**, the four stages resolve to starting state → privilege decision → reachability → proof, and each stage must point to the mechanism statement and evidence in the table below.

| Stage | Mechanism on this topic | Strongest first evidence |
| --- | --- | --- |
| 1. **Starting state** | WSL 1 and WSL 2 have different architecture, kernel, filesystem, and network boundaries. | Source identity and the service or machine induced to authenticate |
| 2. **Privilege decision** | Windows-to-Linux and Linux-to-Windows interoperability exposes path translation, process launching, environment transfer, and mounted-drive policy. | Protocol signing, channel binding, SPN, TLS, and authentication method |
| 3. **Reachability** | distribution registration and per-user virtual disks create artifacts outside the usual Windows process narrative. | Target endpoint authorization and the exact accepted operation |
| 4. **Proof** | monitoring must correlate wsl.exe and wslhost activity, utility VM or Pico process context, network flows, mounted files, and Windows-side consumers. | Host, network, directory, certificate, WMI, or WSL artifacts joined in time |

For **Windows Subsystem for Linux (WSL) as an Attack Surface**, the table is a causal chain rather than a checklist of unrelated facts. Trace it backward from each proof observation to the actor and owned state, then forward through the privilege decision decision, transition, cleanup, and failure path.

## Mechanism deep dive

### 1. Starting state

**Mechanism claim.** WSL 1 and WSL 2 have different architecture, kernel, filesystem, and network boundaries.

For **Windows Subsystem for Linux (WSL) as an Attack Surface**, this stage is where the write-up must become concrete. Capture the caller's actual token and the exact object the caller controls. Do not substitute an administrator-shell result or a scanner label. The relevant evidence source for this stage is **source identity and the service or machine induced to authenticate**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this starting state stage.** For **Windows Subsystem for Linux (WSL) as an Attack Surface**, The standard-user token lacks the claimed control, or the controlled object is not the object used by the consumer. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for starting state.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **Windows Subsystem for Linux (WSL) as an Attack Surface**? If not, this specific mechanism claim remains **UNVERIFIED**.

### 2. Privilege decision

**Mechanism claim.** Windows-to-Linux and Linux-to-Windows interoperability exposes path translation, process launching, environment transfer, and mounted-drive policy.

For **Windows Subsystem for Linux (WSL) as an Attack Surface**, this stage is where the write-up must become concrete. Locate the stronger component that makes the decisive access, parsing, loading, authentication, or execution decision. The relevant evidence source for this stage is **protocol signing, channel binding, spn, tls, and authentication method**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this privilege decision stage.** For **Windows Subsystem for Linux (WSL) as an Attack Surface**, The privileged component never reads, resolves, authenticates, loads, or executes the controlled material. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for privilege decision.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **Windows Subsystem for Linux (WSL) as an Attack Surface**? If not, this specific mechanism claim remains **UNVERIFIED**.

### 3. Reachability

**Mechanism claim.** distribution registration and per-user virtual disks create artifacts outside the usual Windows process narrative.

For **Windows Subsystem for Linux (WSL) as an Attack Surface**, this stage is where the write-up must become concrete. Prove the caller can reach the consumer and trigger on this host without borrowing the privilege the finding is supposed to obtain. The relevant evidence source for this stage is **target endpoint authorization and the exact accepted operation**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this reachability stage.** For **Windows Subsystem for Linux (WSL) as an Attack Surface**, Policy, ACL, path precedence, session, architecture, service state, or trigger timing blocks the chain. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for reachability.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **Windows Subsystem for Linux (WSL) as an Attack Surface**? If not, this specific mechanism claim remains **UNVERIFIED**.

### 4. Proof

**Mechanism claim.** monitoring must correlate wsl.exe and wslhost activity, utility VM or Pico process context, network flows, mounted files, and Windows-side consumers.

For **Windows Subsystem for Linux (WSL) as an Attack Surface**, this stage is where the write-up must become concrete. Join configuration and runtime evidence, then remove one prerequisite and confirm that the predicted privileged transition disappears. The relevant evidence source for this stage is **host, network, directory, certificate, wmi, or wsl artifacts joined in time**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this proof stage.** For **Windows Subsystem for Linux (WSL) as an Attack Surface**, The same effect remains after the required condition is removed, or the resulting context is not stronger than the starting token. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for proof.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **Windows Subsystem for Linux (WSL) as an Attack Surface**? If not, this specific mechanism claim remains **UNVERIFIED**.

## Key terms in plain language

The terms below are intentionally scoped to **Windows Subsystem for Linux (WSL) as an Attack Surface**. They should not be used as synonyms: each names a different object, policy, boundary, or kind of evidence.

| Term | Precise working meaning |
| --- | --- |
| **Mechanism** | The concrete state, component, decision, and transition that produce an observable result. |
| **Trust boundary** | A point where data or control crosses into a component with different authority, ownership, or assumptions. |
| **Evidence** | A recorded observation with enough identity, time, build, provenance, and collection context to support or challenge a claim. |
| **Negative control** | A repeat of the experiment with one required condition removed, used to test causality and alternate explanations. |

When public documentation and debugger terminology for **Windows Subsystem for Linux (WSL) as an Attack Surface** differ, retain both names and state which source supplied each one. Any debugger field used in this lesson is evidence for the recorded build, not automatically a stable ABI or security contract.

## Version and environment scope

Protocol hardening, endpoint configuration, domain functional state, certificate templates, DCOM changes, WSL architecture, and cumulative updates alter the chain. For **Windows Subsystem for Linux (WSL) as an Attack Surface**, the version-sensitive statement that must be rechecked first is: distribution registration and per-user virtual disks create artifacts outside the usual Windows process narrative.

Record the following **Windows Subsystem for Linux (WSL) as an Attack Surface** dimensions before comparing two systems or importing an exploitability conclusion from another target:

| Dimension | Minimum record | Why it can change the answer |
| --- | --- | --- |
| Operating system | Edition, version, build, revision, update level | Code paths, defaults, layouts, services, telemetry, and mitigations change through servicing. |
| Architecture | x86, x64, ARM64, native or WoW64 view | Pointer width, calling convention, structure view, redirection, and available mitigations differ. |
| Identity | User and group SIDs, integrity, privileges, process token, thread token, session | The actor that can observe a condition may not be the actor that can reach or trigger it. |
| Policy | Effective local/domain/MDM/runtime source and enforcement mode | A visible registry value or UI setting may be overridden, merged, audited, or inactive. |
| Mitigations | VBS/HVCI, CFG/CET, DEP, ASLR, application control, PPL, sandbox policy as relevant | Exploitability and observability depend on actual opt-in, hardware, boot, and process state. |
| Symbols and tools | PDB identity, symbol path, debugger/parser/collector version | Offsets, names, schemas, and decoded fields are meaningful only with matching provenance. |
| Sensor health | Provider/session/channel, filters, loss, clock, retention, forwarding | Missing telemetry cannot support absence until a benign control proves the collection path. |
| Lifetime | Creation, references, asynchronous work, cancellation, teardown, cleanup | Stale handles, reused identifiers, queued callbacks, and delayed consumers can change object identity. |

Unknown values in a **Windows Subsystem for Linux (WSL) as an Attack Surface** decision record must remain explicitly **unknown**. Do not fill this article's gaps with defaults copied from documentation or another researcher's machine.

## Evidence matrix

For **Windows Subsystem for Linux (WSL) as an Attack Surface**, work from each mechanism claim to the least-invasive evidence source before choosing a tool:

| Claim stage | Preferred evidence | Observation that supports the claim | Observation that falsifies or narrows it |
| --- | --- | --- | --- |
| Starting state | Source identity and the service or machine induced to authenticate | WSL 1 and WSL 2 have different architecture, kernel, filesystem, and network boundaries. | The standard-user token lacks the claimed control, or the controlled object is not the object used by the consumer. |
| Privilege decision | Protocol signing, channel binding, SPN, TLS, and authentication method | Windows-to-Linux and Linux-to-Windows interoperability exposes path translation, process launching, environment transfer, and mounted-drive policy. | The privileged component never reads, resolves, authenticates, loads, or executes the controlled material. |
| Reachability | Target endpoint authorization and the exact accepted operation | distribution registration and per-user virtual disks create artifacts outside the usual Windows process narrative. | Policy, ACL, path precedence, session, architecture, service state, or trigger timing blocks the chain. |
| Proof | Host, network, directory, certificate, WMI, or WSL artifacts joined in time | monitoring must correlate wsl.exe and wslhost activity, utility VM or Pico process context, network flows, mounted files, and Windows-side consumers. | The same effect remains after the required condition is removed, or the resulting context is not stronger than the starting token. |

For **Windows Subsystem for Linux (WSL) as an Attack Surface**, a screenshot of one promising field is weaker than a reproducible evidence bundle. Preserve raw commands, complete output, exit status, actor token, object identity, timestamps, build, symbol or decoder provenance, and both controls named above.

## Safe guided lab

Use a disposable Windows 11 virtual machine with a standard-user test account and a separate administrator account. Inspect only resources you create or systems you are explicitly authorized to assess. The exercises collect configuration and telemetry; they do not deploy an escalation payload. The **Windows Subsystem for Linux (WSL) as an Attack Surface** exercise is limited to the read-only inspection probe and controls described below.

### 1. Establish the baseline

Run the **Windows Subsystem for Linux (WSL) as an Attack Surface** baseline from the identity and architecture named in the hypothesis. Use a fresh snapshot or purpose-built test object for this mechanism and save the output with an ISO-8601 timestamp.

```powershell
# Run from a standard-user PowerShell console
whoami /all
Get-ComputerInfo | Select-Object WindowsProductName, WindowsVersion, OsBuildNumber
Get-CimInstance Win32_OperatingSystem | Select-Object Caption, Version, BuildNumber
Get-Date -Format o
```

Baseline acceptance criteria for **Windows Subsystem for Linux (WSL) as an Attack Surface**:

- the **Windows Subsystem for Linux (WSL) as an Attack Surface** execution identity and Windows build are visible in the saved record;
- the test object, file, process, endpoint, provider, or specimen belongs to the lab;
- relevant policy and mitigation values are captured before the observation;
- no production secret, unknown binary, persistence payload, or destructive trigger is introduced.

### 2. Inspect the topic-specific mechanism

The **Windows Subsystem for Linux (WSL) as an Attack Surface** probe below is read-only. Replace placeholders only with a process, file, object, trace, or VM that belongs to the authorized lab. If an elevated debugger is needed to observe this mechanism, record that elevation separately; it does not prove a standard user can cause the same transition.

```text
wsl.exe --status
wsl.exe --list --verbose
Get-WinEvent -ListLog *WSL* -ErrorAction SilentlyContinue
```

Interpret the **Windows Subsystem for Linux (WSL) as an Attack Surface** output against starting state, privilege decision, reachability, proof rather than a tool's severity label. If a field or command is unavailable on this target, record that as a version/tool result and do not import the missing value from another build.

### 3. Run a positive and negative control

For **Windows Subsystem for Linux (WSL) as an Attack Surface**, the positive control keeps every required precondition and predicts the proof observation. The negative control changes exactly one relevant variable—actor identity, object ACL, policy, architecture/view, consumer state, trigger, network response, or sensor configuration—while the remaining lab state stays stable.

| Control | Change | Expected result |
| --- | --- | --- |
| Positive | Preserve all four mechanism stages | The predicted transition appears in the primary and at least one corroborating source. |
| Negative | Remove one required condition | The transition disappears or changes in the way predicted by the model. |
| Sensor health | Generate a benign event with a known result | The collector records and decodes it without unexplained loss. |
| Cleanup | Revert the test object and repeat inspection | Persistent state returns to baseline and no queued work remains. |

### 4. Write the decision record

```text
Title: Windows Subsystem for Linux (WSL) as an Attack Surface
Claim:
Target: edition | version | build.revision | architecture
Identity: process token | thread token | integrity | privileges | session
Starting state:
Decision boundary:
Required condition:
Observed transition:
Primary evidence:
Corroborating evidence:
Contradictory evidence:
Benign control:
Negative control:
Version and policy caveats:
Cleanup result:
Confidence: CONFIRMED | LIKELY | UNVERIFIED | REJECTED
Next experiment and falsification criterion:
```

The **Windows Subsystem for Linux (WSL) as an Attack Surface** lab is complete only when the controls explain its evidence. A dramatic tool label, isolated event, writable path, suspicious string, or debugger field cannot substitute for the four-stage mechanism.

## Worked case-study method

A scanner reports a possible elevation path after finding one suspicious permission or policy value. For **Windows Subsystem for Linux (WSL) as an Attack Surface**, treat that clue as a lead and review it through the same four stages:

1. **Starting state:** verify the current-target statement that WSL 1 and WSL 2 have different architecture, kernel, filesystem, and network boundaries.
2. **Privilege decision:** identify the component and decision described by this claim: Windows-to-Linux and Linux-to-Windows interoperability exposes path translation, process launching, environment transfer, and mounted-drive policy.
3. **Reachability:** test the build, identity, policy, lifetime, and configuration constraint: distribution registration and per-user virtual disks create artifacts outside the usual Windows process narrative.
4. **Proof:** demand the joined observation described here: monitoring must correlate wsl.exe and wslhost activity, utility VM or Pico process context, network flows, mounted files, and Windows-side consumers.

For **Windows Subsystem for Linux (WSL) as an Attack Surface**, stop at the strongest supported conclusion. If starting state and privilege decision are proven but reachability is not, report a **dangerous condition** or **research lead**, not confirmed impact. If runtime evidence agrees but the sensor-health control fails, lower confidence. If the negative control does not change the result, revise this mechanism before collecting more screenshots.

A publication-quality **Windows Subsystem for Linux (WSL) as an Attack Surface** case study should include a target table, causal diagram, exact evidence excerpts, competing explanations, version scope, mitigation interaction, cleanup, limitations, and direct references. A second researcher must be able to reproduce this mechanism without relying on the original author's private intuition.

## Detection and hardening

- For **Windows Subsystem for Linux (WSL) as an Attack Surface**, record the writer identity and the exact object changed.
- Correlate that change with the privileged consumer and its trigger.
- Alert on the joined transition rather than a utility name by itself.
- Harden the earliest reachable edge: permissions, policy, trigger, or consumer identity.

For the **Windows Subsystem for Linux (WSL) as an Attack Surface** detection model, correlate source identity and the service or machine induced to authenticate, protocol signing, channel binding, spn, tls, and authentication method, target endpoint authorization and the exact accepted operation, host, network, directory, certificate, wmi, or wsl artifacts joined in time. Join its state change to consumer activity and resulting effect within a justified time window, then document required fields, clock source, exclusions, expected volume, sensor-health control, and what this analytic cannot prove.

Hardening **Windows Subsystem for Linux (WSL) as an Attack Surface** should break the earliest edge that is both reachable and operationally safe to change. After changing the relevant permission, policy, service exposure, execution control, authentication, egress, or telemetry path, repeat this lesson's controls. **Mitigated** means the predicted transition no longer occurs under the original preconditions—not merely that a setting changed.

## Failure modes and review checklist

Use this checklist when reviewing a draft of **Windows Subsystem for Linux (WSL) as an Attack Surface**:

- Does the opening of **Windows Subsystem for Linux (WSL) as an Attack Surface** state the exact target, actor, object or artifact, consumer, constraint, and result?
- Are documented facts, current-target observations, and implementation inferences labeled separately?
- Does every internal structure, event schema, default, or policy claim carry build and source provenance?
- Is a handle or identifier kept separate from the underlying object and its lifetime?
- Is writable or suspicious state kept separate from consumer reachability and actual execution?
- Are static capability, configured capability, and behavior observed in one execution reported at different confidence levels?
- Does a benign control prove the sensor, and does a negative control test causality?
- Are cancellation, teardown, cleanup, failure behavior, and asynchronous work included?
- Are mitigations recorded as effective target state rather than assumed from product version?
- Could another researcher falsify the conclusion using the retained evidence?

A **Windows Subsystem for Linux (WSL) as an Attack Surface** draft that cannot answer these questions is not finished, regardless of its word count or number of screenshots.

## Takeaways

- **Starting state:** WSL 1 and WSL 2 have different architecture, kernel, filesystem, and network boundaries.
- **Privilege decision:** Windows-to-Linux and Linux-to-Windows interoperability exposes path translation, process launching, environment transfer, and mounted-drive policy.
- **Reachability:** distribution registration and per-user virtual disks create artifacts outside the usual Windows process narrative.
- **Proof:** monitoring must correlate wsl.exe and wslhost activity, utility VM or Pico process context, network flows, mounted files, and Windows-side consumers.

The durable **Windows Subsystem for Linux (WSL) as an Attack Surface** skill is not remembering one command. It is connecting each of this lesson's four mechanism statements to a target-specific observation, explaining what would disprove it, and repeating the test without silently changing the trust boundary.

## Check your understanding

<details>
  <summary>Can you explain Windows Subsystem for Linux (WSL) as an Attack Surface without naming a tool?</summary>
  A strong answer follows lower-privileged actor → controlled input → privileged consumer → trigger → measurable effect. It names the owner or actor, controlled state, deciding component, required condition, transition, lifetime, and evidence.
</details>

<details>
  <summary>Which fact is most likely to change across Windows versions?</summary>
  For **Windows Subsystem for Linux (WSL) as an Attack Surface**, start with this track warning: Protocol hardening, endpoint configuration, domain functional state, certificate templates, DCOM changes, WSL architecture, and cumulative updates alter the chain. Then
  replace the assumption with a current-target observation tied to
  **WSL 1 and WSL 2 have different architecture, kernel, filesystem, and network boundaries**.
</details>

<details>
  <summary>What result would make you reject the model?</summary>
  Reject or revise the **Windows Subsystem for Linux (WSL) as an Attack Surface** model when its predicted transition survives
  removal of a required condition, the stated actor cannot reach
  **Windows-to-Linux and Linux-to-Windows interoperability exposes path translation, process launching, environment transfer, and mounted-drive policy**, object identity or lifetime is wrong, or an
  independent healthy source contradicts the claimed state.
</details>

## Limitations and next research

This **Windows Subsystem for Linux (WSL) as an Attack Surface** document does not claim that every Windows edition, patch level, architecture, policy set, hardware configuration, or product integration behaves identically. Its read-only lab is not exploitability proof, attribution, or a production authorization decision. The next useful experiment is the smallest safe observation that can move one named starting state/privilege decision/reachability/proof stage from **UNVERIFIED** to **LIKELY** or **CONFIRMED**, or reject it cleanly.

## References

### Source coverage and reliability

The **Windows Subsystem for Linux (WSL) as an Attack Surface** lesson consolidates the source material below. These pages and repositories provide context but cannot override current-target evidence. Any public structure layout, ReactOS/Wine behavior, debugger screenshot, blog-derived claim, or cross-build offset used here remains **UNVERIFIED** until checked against the tested binary, symbols, documented API, dump, or runtime trace.

| Source | Type | How it is used |
| --- | --- | --- |
| [Windows Subsystem for Linux (WSL) as an Attack Surface](https://yunolay.com/windows-subsystem-for-linux-wsl-as-an-attack-surface/) | Original lesson source | Establishes the source article's terminology and examples; re-check claims against primary documentation and the tested build. |
| [learn.microsoft.com/windows/security/identity-protection/access-control/access-control](https://learn.microsoft.com/windows/security/identity-protection/access-control/access-control) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [learn.microsoft.com/windows/win32/secauthz/access-control](https://learn.microsoft.com/windows/win32/secauthz/access-control) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [learn.microsoft.com/windows-server/identity/ad-ds/manage/understand-security-identifiers](https://learn.microsoft.com/windows-server/identity/ad-ds/manage/understand-security-identifiers) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [learn.microsoft.com/sysinternals/](https://learn.microsoft.com/sysinternals/) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [learn.microsoft.com/windows/security/application-security/application-control/windows-defender-application-control/](https://learn.microsoft.com/windows/security/application-security/application-control/windows-defender-application-control/) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [attack.mitre.org/tactics/TA0004/](https://attack.mitre.org/tactics/TA0004/) | Knowledge base | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [www.microsoft.com/msrc/security-guidance](https://msrc.microsoft.com/update-guide/) | Research reference | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [github.com/gtworek/Priv2Admin](https://github.com/gtworek/Priv2Admin) | Source repository | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |

Accessed: **2026-07-28**.
