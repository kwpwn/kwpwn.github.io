---
title: "UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse"
description: "A plain-language, evidence-first guide to UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse."
locale: "en"
publishDate: 2026-07-28
updatedAt: 2026-07-28
draft: false
featured: false
tags:
  - "Windows Privilege Escalation"
  - "token ipc"
  - "Research Notes"
author: "Kwpwn"
content_type: "concept"
status: "preliminary"
evidence_level: "unverified"
toc: true
wide: false
topic: "windows-privesc"
track: "token-ipc"
seriesOrder: 17
topicPlacements:
  - topic: "windows-privesc"
    track: "token-ipc"
    order: 17
difficulty: "Intermediate"
prerequisites:
  - "Basic Windows access-control and process concepts"
  - "A disposable VM with a standard-user test account"
learningObjectives:
  - "Build a clear mental model for UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse"
  - "Identify the state, decision boundary, required condition, and observable proof"
  - "Verify the model with a read-only probe and a meaningful negative control"
labEnvironment: "Use a disposable Windows 11 virtual machine with a standard-user test account and a separate administrator account. Inspect only resources you create or systems you are explicitly authorized to assess. The exercises collect configuration and telemetry; they do not deploy an escalation payload."
---

> **Quick answer:** UAC splits an administrator's interactive logon into linked token contexts and mediates elevation consent. The conclusion becomes defensible only when the remaining boundary, constraint, and evidence stages are verified on the named build and identity.

## Scope and evidence contract

This lesson treats local elevation as a verifiable trust-boundary chain. It does not begin with a payload. It begins with the caller's real token, the object the caller can influence, the privileged component that consumes that object, and the operation that would produce a stronger security context. Applied to **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, this method begins with the concrete statement that uac splits an administrator's interactive logon into linked token contexts and mediates elevation consent.

This document treats **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** as a mechanism to reconstruct, not a label to memorize. Its curriculum placement is windows-privesc lesson 17 in token-ipc. That placement helps navigation; it does not imply that the behavior belongs to one security domain or that every environment exposes the same path.

The **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** track-specific focus is: Treat impersonation chains as identity transfer across a real IPC or authentication boundary. A privileged client connection, permitted impersonation level, token type, and usable privilege are separate prerequisites. The practical objective for this article is to name the exact coercion and impersonation primitives instead of reporting a potato-family tool name as the mechanism.

Before using any internal field, configuration value, event, or public write-up as evidence for **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, record the target's Windows edition, version, build and revision, architecture, symbol status, relevant policy and mitigation state, execution identity, and collection time. The confidence vocabulary below prevents this specific lesson from hiding uncertainty behind polished prose:

| State | Meaning in this lesson |
| --- | --- |
| **CONFIRMED** | Current-target evidence proves the claim and the expected negative control changes the result. |
| **LIKELY** | Multiple sources support the claim, but one decisive low-risk observation remains unavailable. |
| **UNVERIFIED** | The statement is plausible but depends on public references, a decompiler view, a scanner label, another build, or an untested configuration. |
| **REJECTED** | Current evidence or a control contradicts the proposed mechanism. |

## Architecture at a glance

Use this flow as the shortest complete map:

> **lower-privileged actor → controlled input → privileged consumer → trigger → measurable effect**

A writable object is only the first edge. The chain is incomplete until the privileged consumer, trigger, resulting context, and negative control are all known. In **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, the four stages resolve to starting state → privilege decision → reachability → proof, and each stage must point to the mechanism statement and evidence in the table below.

| Stage | Mechanism on this topic | Strongest first evidence |
| --- | --- | --- |
| 1. **Starting state** | UAC splits an administrator's interactive logon into linked token contexts and mediates elevation consent. | Server endpoint ACL and attacker process/thread token |
| 2. **Privilege decision** | auto-elevation is granted only to eligible signed components and is influenced by manifest, policy, integrity, and invocation path. | Origin and security context of the connecting privileged client |
| 3. **Reachability** | the secure desktop and consent behavior vary with UAC policy and account type. | Impersonation level, token type, privileges, session, and restrictions |
| 4. **Proof** | UAC is not a boundary against an already-compromised administrator, but it remains a meaningful control against silent elevation and accidental privilege use. | Revert, duplicate, access-check, and negative-control observations |

For **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, the table is a causal chain rather than a checklist of unrelated facts. Trace it backward from each proof observation to the actor and owned state, then forward through the privilege decision decision, transition, cleanup, and failure path.

## Mechanism deep dive

### 1. Starting state

**Mechanism claim.** UAC splits an administrator's interactive logon into linked token contexts and mediates elevation consent.

For **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, this stage is where the write-up must become concrete. Capture the caller's actual token and the exact object the caller controls. Do not substitute an administrator-shell result or a scanner label. The relevant evidence source for this stage is **server endpoint acl and attacker process/thread token**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this starting state stage.** For **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, The standard-user token lacks the claimed control, or the controlled object is not the object used by the consumer. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for starting state.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**? If not, this specific mechanism claim remains **UNVERIFIED**.

### 2. Privilege decision

**Mechanism claim.** auto-elevation is granted only to eligible signed components and is influenced by manifest, policy, integrity, and invocation path.

For **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, this stage is where the write-up must become concrete. Locate the stronger component that makes the decisive access, parsing, loading, authentication, or execution decision. The relevant evidence source for this stage is **origin and security context of the connecting privileged client**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this privilege decision stage.** For **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, The privileged component never reads, resolves, authenticates, loads, or executes the controlled material. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for privilege decision.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**? If not, this specific mechanism claim remains **UNVERIFIED**.

### 3. Reachability

**Mechanism claim.** the secure desktop and consent behavior vary with UAC policy and account type.

For **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, this stage is where the write-up must become concrete. Prove the caller can reach the consumer and trigger on this host without borrowing the privilege the finding is supposed to obtain. The relevant evidence source for this stage is **impersonation level, token type, privileges, session, and restrictions**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this reachability stage.** For **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, Policy, ACL, path precedence, session, architecture, service state, or trigger timing blocks the chain. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for reachability.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**? If not, this specific mechanism claim remains **UNVERIFIED**.

### 4. Proof

**Mechanism claim.** UAC is not a boundary against an already-compromised administrator, but it remains a meaningful control against silent elevation and accidental privilege use.

For **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, this stage is where the write-up must become concrete. Join configuration and runtime evidence, then remove one prerequisite and confirm that the predicted privileged transition disappears. The relevant evidence source for this stage is **revert, duplicate, access-check, and negative-control observations**. Preserve the complete result rather than copying only the field that appears to support the hypothesis.

**What can invalidate this proof stage.** For **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, The same effect remains after the required condition is removed, or the resulting context is not stronger than the starting token. Treat that result as useful evidence because it narrows this article's mechanism and prevents the lead from becoming a universal security claim.

**Review question for proof.** Can another researcher identify the actor or owner, exact object or artifact, deciding component, required state, and timestamp for this stage of **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**? If not, this specific mechanism claim remains **UNVERIFIED**.

## Key terms in plain language

The terms below are intentionally scoped to **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**. They should not be used as synonyms: each names a different object, policy, boundary, or kind of evidence.

| Term | Precise working meaning |
| --- | --- |
| **UAC** | The elevation-consent system that mediates use of an administrator's linked filtered and elevated tokens. |
| **Mechanism** | The concrete state, component, decision, and transition that produce an observable result. |
| **Trust boundary** | A point where data or control crosses into a component with different authority, ownership, or assumptions. |
| **Evidence** | A recorded observation with enough identity, time, build, provenance, and collection context to support or challenge a claim. |
| **Negative control** | A repeat of the experiment with one required condition removed, used to test causality and alternate explanations. |

When public documentation and debugger terminology for **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** differ, retain both names and state which source supplied each one. Any debugger field used in this lesson is evidence for the recorded build, not automatically a stable ABI or security contract.

## Version and environment scope

COM/RPC activation, authentication protections, session isolation, service privileges, token restrictions, and patched coercion paths invalidate many named technique variants. For **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, the version-sensitive statement that must be rechecked first is: the secure desktop and consent behavior vary with UAC policy and account type.

Record the following **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** dimensions before comparing two systems or importing an exploitability conclusion from another target:

| Dimension | Minimum record | Why it can change the answer |
| --- | --- | --- |
| Operating system | Edition, version, build, revision, update level | Code paths, defaults, layouts, services, telemetry, and mitigations change through servicing. |
| Architecture | x86, x64, ARM64, native or WoW64 view | Pointer width, calling convention, structure view, redirection, and available mitigations differ. |
| Identity | User and group SIDs, integrity, privileges, process token, thread token, session | The actor that can observe a condition may not be the actor that can reach or trigger it. |
| Policy | Effective local/domain/MDM/runtime source and enforcement mode | A visible registry value or UI setting may be overridden, merged, audited, or inactive. |
| Mitigations | VBS/HVCI, CFG/CET, DEP, ASLR, application control, PPL, sandbox policy as relevant | Exploitability and observability depend on actual opt-in, hardware, boot, and process state. |
| Symbols and tools | PDB identity, symbol path, debugger/parser/collector version | Offsets, names, schemas, and decoded fields are meaningful only with matching provenance. |
| Sensor health | Provider/session/channel, filters, loss, clock, retention, forwarding | Missing telemetry cannot support absence until a benign control proves the collection path. |
| Lifetime | Creation, references, asynchronous work, cancellation, teardown, cleanup | Stale handles, reused identifiers, queued callbacks, and delayed consumers can change object identity. |

Unknown values in a **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** decision record must remain explicitly **unknown**. Do not fill this article's gaps with defaults copied from documentation or another researcher's machine.

## Evidence matrix

For **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, work from each mechanism claim to the least-invasive evidence source before choosing a tool:

| Claim stage | Preferred evidence | Observation that supports the claim | Observation that falsifies or narrows it |
| --- | --- | --- | --- |
| Starting state | Server endpoint ACL and attacker process/thread token | UAC splits an administrator's interactive logon into linked token contexts and mediates elevation consent. | The standard-user token lacks the claimed control, or the controlled object is not the object used by the consumer. |
| Privilege decision | Origin and security context of the connecting privileged client | auto-elevation is granted only to eligible signed components and is influenced by manifest, policy, integrity, and invocation path. | The privileged component never reads, resolves, authenticates, loads, or executes the controlled material. |
| Reachability | Impersonation level, token type, privileges, session, and restrictions | the secure desktop and consent behavior vary with UAC policy and account type. | Policy, ACL, path precedence, session, architecture, service state, or trigger timing blocks the chain. |
| Proof | Revert, duplicate, access-check, and negative-control observations | UAC is not a boundary against an already-compromised administrator, but it remains a meaningful control against silent elevation and accidental privilege use. | The same effect remains after the required condition is removed, or the resulting context is not stronger than the starting token. |

For **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, a screenshot of one promising field is weaker than a reproducible evidence bundle. Preserve raw commands, complete output, exit status, actor token, object identity, timestamps, build, symbol or decoder provenance, and both controls named above.

## Safe guided lab

Use a disposable Windows 11 virtual machine with a standard-user test account and a separate administrator account. Inspect only resources you create or systems you are explicitly authorized to assess. The exercises collect configuration and telemetry; they do not deploy an escalation payload. The **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** exercise is limited to the read-only inspection probe and controls described below.

### 1. Establish the baseline

Run the **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** baseline from the identity and architecture named in the hypothesis. Use a fresh snapshot or purpose-built test object for this mechanism and save the output with an ISO-8601 timestamp.

```powershell
# Run from a standard-user PowerShell console
whoami /all
Get-ComputerInfo | Select-Object WindowsProductName, WindowsVersion, OsBuildNumber
Get-CimInstance Win32_OperatingSystem | Select-Object Caption, Version, BuildNumber
Get-Date -Format o
```

Baseline acceptance criteria for **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**:

- the **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** execution identity and Windows build are visible in the saved record;
- the test object, file, process, endpoint, provider, or specimen belongs to the lab;
- relevant policy and mitigation values are captured before the observation;
- no production secret, unknown binary, persistence payload, or destructive trigger is introduced.

### 2. Inspect the topic-specific mechanism

The **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** probe below is read-only. Replace placeholders only with a process, file, object, trace, or VM that belongs to the authorized lab. If an elevated debugger is needed to observe this mechanism, record that elevation separately; it does not prove a standard user can cause the same transition.

```text
whoami /groups
Get-ItemProperty "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System" |
  Select-Object EnableLUA, ConsentPromptBehaviorAdmin, PromptOnSecureDesktop
```

Interpret the **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** output against starting state, privilege decision, reachability, proof rather than a tool's severity label. If a field or command is unavailable on this target, record that as a version/tool result and do not import the missing value from another build.

### 3. Run a positive and negative control

For **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, the positive control keeps every required precondition and predicts the proof observation. The negative control changes exactly one relevant variable—actor identity, object ACL, policy, architecture/view, consumer state, trigger, network response, or sensor configuration—while the remaining lab state stays stable.

| Control | Change | Expected result |
| --- | --- | --- |
| Positive | Preserve all four mechanism stages | The predicted transition appears in the primary and at least one corroborating source. |
| Negative | Remove one required condition | The transition disappears or changes in the way predicted by the model. |
| Sensor health | Generate a benign event with a known result | The collector records and decodes it without unexplained loss. |
| Cleanup | Revert the test object and repeat inspection | Persistent state returns to baseline and no queued work remains. |

### 4. Write the decision record

```text
Title: UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse
Claim:
Target: edition | version | build.revision | architecture
Identity: process token | thread token | integrity | privileges | session
Starting state:
Decision boundary:
Required condition:
Observed transition:
Primary evidence:
Corroborating evidence:
Contradictory evidence:
Benign control:
Negative control:
Version and policy caveats:
Cleanup result:
Confidence: CONFIRMED | LIKELY | UNVERIFIED | REJECTED
Next experiment and falsification criterion:
```

The **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** lab is complete only when the controls explain its evidence. A dramatic tool label, isolated event, writable path, suspicious string, or debugger field cannot substitute for the four-stage mechanism.

## Worked case-study method

A scanner reports a possible elevation path after finding one suspicious permission or policy value. For **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, treat that clue as a lead and review it through the same four stages:

1. **Starting state:** verify the current-target statement that UAC splits an administrator's interactive logon into linked token contexts and mediates elevation consent.
2. **Privilege decision:** identify the component and decision described by this claim: auto-elevation is granted only to eligible signed components and is influenced by manifest, policy, integrity, and invocation path.
3. **Reachability:** test the build, identity, policy, lifetime, and configuration constraint: the secure desktop and consent behavior vary with UAC policy and account type.
4. **Proof:** demand the joined observation described here: UAC is not a boundary against an already-compromised administrator, but it remains a meaningful control against silent elevation and accidental privilege use.

For **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, stop at the strongest supported conclusion. If starting state and privilege decision are proven but reachability is not, report a **dangerous condition** or **research lead**, not confirmed impact. If runtime evidence agrees but the sensor-health control fails, lower confidence. If the negative control does not change the result, revise this mechanism before collecting more screenshots.

A publication-quality **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** case study should include a target table, causal diagram, exact evidence excerpts, competing explanations, version scope, mitigation interaction, cleanup, limitations, and direct references. A second researcher must be able to reproduce this mechanism without relying on the original author's private intuition.

## Detection and hardening

- For **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, record the writer identity and the exact object changed.
- Correlate that change with the privileged consumer and its trigger.
- Alert on the joined transition rather than a utility name by itself.
- Harden the earliest reachable edge: permissions, policy, trigger, or consumer identity.

For the **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** detection model, correlate server endpoint acl and attacker process/thread token, origin and security context of the connecting privileged client, impersonation level, token type, privileges, session, and restrictions, revert, duplicate, access-check, and negative-control observations. Join its state change to consumer activity and resulting effect within a justified time window, then document required fields, clock source, exclusions, expected volume, sensor-health control, and what this analytic cannot prove.

Hardening **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** should break the earliest edge that is both reachable and operationally safe to change. After changing the relevant permission, policy, service exposure, execution control, authentication, egress, or telemetry path, repeat this lesson's controls. **Mitigated** means the predicted transition no longer occurs under the original preconditions—not merely that a setting changed.

## Failure modes and review checklist

Use this checklist when reviewing a draft of **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**:

- Does the opening of **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** state the exact target, actor, object or artifact, consumer, constraint, and result?
- Are documented facts, current-target observations, and implementation inferences labeled separately?
- Does every internal structure, event schema, default, or policy claim carry build and source provenance?
- Is a handle or identifier kept separate from the underlying object and its lifetime?
- Is writable or suspicious state kept separate from consumer reachability and actual execution?
- Are static capability, configured capability, and behavior observed in one execution reported at different confidence levels?
- Does a benign control prove the sensor, and does a negative control test causality?
- Are cancellation, teardown, cleanup, failure behavior, and asynchronous work included?
- Are mitigations recorded as effective target state rather than assumed from product version?
- Could another researcher falsify the conclusion using the retained evidence?

A **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** draft that cannot answer these questions is not finished, regardless of its word count or number of screenshots.

## Takeaways

- **Starting state:** UAC splits an administrator's interactive logon into linked token contexts and mediates elevation consent.
- **Privilege decision:** auto-elevation is granted only to eligible signed components and is influenced by manifest, policy, integrity, and invocation path.
- **Reachability:** the secure desktop and consent behavior vary with UAC policy and account type.
- **Proof:** UAC is not a boundary against an already-compromised administrator, but it remains a meaningful control against silent elevation and accidental privilege use.

The durable **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** skill is not remembering one command. It is connecting each of this lesson's four mechanism statements to a target-specific observation, explaining what would disprove it, and repeating the test without silently changing the trust boundary.

## Check your understanding

<details>
  <summary>Can you explain UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse without naming a tool?</summary>
  A strong answer follows lower-privileged actor → controlled input → privileged consumer → trigger → measurable effect. It names the owner or actor, controlled state, deciding component, required condition, transition, lifetime, and evidence.
</details>

<details>
  <summary>Which fact is most likely to change across Windows versions?</summary>
  For **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse**, start with this track warning: COM/RPC activation, authentication protections, session isolation, service privileges, token restrictions, and patched coercion paths invalidate many named technique variants. Then
  replace the assumption with a current-target observation tied to
  **UAC splits an administrator's interactive logon into linked token contexts and mediates elevation consent**.
</details>

<details>
  <summary>What result would make you reject the model?</summary>
  Reject or revise the **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** model when its predicted transition survives
  removal of a required condition, the stated actor cannot reach
  **auto-elevation is granted only to eligible signed components and is influenced by manifest, policy, integrity, and invocation path**, object identity or lifetime is wrong, or an
  independent healthy source contradicts the claimed state.
</details>

## Limitations and next research

This **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** document does not claim that every Windows edition, patch level, architecture, policy set, hardware configuration, or product integration behaves identically. Its read-only lab is not exploitability proof, attribution, or a production authorization decision. The next useful experiment is the smallest safe observation that can move one named starting state/privilege decision/reachability/proof stage from **UNVERIFIED** to **LIKELY** or **CONFIRMED**, or reject it cleanly.

## References

### Source coverage and reliability

The **UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse** lesson consolidates the source material below. These pages and repositories provide context but cannot override current-target evidence. Any public structure layout, ReactOS/Wine behavior, debugger screenshot, blog-derived claim, or cross-build offset used here remains **UNVERIFIED** until checked against the tested binary, symbols, documented API, dump, or runtime trace.

| Source | Type | How it is used |
| --- | --- | --- |
| [UAC Bypass Techniques: A Practical Overview of Auto-Elevation Abuse](https://yunolay.com/uac-bypass/) | Original lesson source | Establishes the source article's terminology and examples; re-check claims against primary documentation and the tested build. |
| [UAC Bypass Techniques via Auto-Elevating Binaries](https://yunolay.com/uac-bypass-techniques-via-auto-elevating-binaries/) | Original lesson source | Establishes the source article's terminology and examples; re-check claims against primary documentation and the tested build. |
| [learn.microsoft.com/windows/security/identity-protection/access-control/access-control](https://learn.microsoft.com/windows/security/identity-protection/access-control/access-control) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [learn.microsoft.com/windows/win32/secauthz/access-control](https://learn.microsoft.com/windows/win32/secauthz/access-control) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [learn.microsoft.com/windows-server/identity/ad-ds/manage/understand-security-identifiers](https://learn.microsoft.com/windows-server/identity/ad-ds/manage/understand-security-identifiers) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [learn.microsoft.com/sysinternals/](https://learn.microsoft.com/sysinternals/) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [learn.microsoft.com/windows/security/application-security/application-control/windows-defender-application-control/](https://learn.microsoft.com/windows/security/application-security/application-control/windows-defender-application-control/) | Microsoft documentation | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [attack.mitre.org/tactics/TA0004/](https://attack.mitre.org/tactics/TA0004/) | Knowledge base | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [www.microsoft.com/msrc/security-guidance](https://msrc.microsoft.com/update-guide/) | Research reference | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |
| [github.com/gtworek/Priv2Admin](https://github.com/gtworek/Priv2Admin) | Source repository | Use for contracts, terminology, tooling, or comparative context; it is not proof of the current target by itself. |

Accessed: **2026-07-28**.
